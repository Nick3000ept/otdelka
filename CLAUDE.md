# CLAUDE.md — операционная шпаргалка

> 📍 **[CODE_MAP.md](CODE_MAP.md)** — карта функций и кода `index.html`/`script.gs`
> с номерами строк. Перед правкой смотреть туда, а не читать файлы целиком.

Контекст для будущих сессий Claude в этой папке. Общие правила — в `VS_hub/CLAUDE.md` (корень), здесь только специфика проекта.

## Проект

Витрина расценок по отделке СБ3 — веб-страница только для просмотра (без редактирования).
Состояние на конец 2026-08-03 (за день переделано почти всё, детали в разделах ниже):

- **Боковая панель с вкладками** (стиль КЛАД): **Расценки** / **Объемы** / блок **Проверки**.
- **Расценки**: дерево работ Место→Поверхность→Группа→Работа (2/3 экрана) + нижняя панель
  (1/3): слева сводная «подрядчик × корпус» с блоком «Собственные силы» и строкой «Базовая
  расценка», справа материалы работы. Клик по работе наполняет нижнюю панель; клик по
  подрядчику — модальное окно с деталями из «Расценок подрядчиков» / «Расценок СС».
  Над сводной — переключатель ячеек **Работа / Материалы / Работа + материалы**
  (04.08.2026): работа — «Расценка за работу на ед в бюджет», материалы — «Стоимость
  материалов на ед в бюджет»; строка «Базовая расценка» переключается синхронно
  (баз. раб. / баз. мат. / сумма).
- ⚠️ **Колонка «Объем работ» на «Расценках»** (и сумматоры групп) с 2026-08-05 показывает
  ЧИСТЫЙ объём из поэтажки (кол. D, сумма по корпусам, `workVolD`), а не значение из
  справочника «Работы» — там объём с повышающим коэффициентом. Для этого action=volumes
  грузится фоном при старте; пока грузится — в колонке «…», затем дорисовывается.
  **Если работы в поэтажке нет вовсе** — запасной объём из справочника «Работы»,
  приглушённым цветом и с подсказкой при наведении, что значение с коэффициентом
  (жалоба пользователя «не отображаются объёмы» 2026-08-05: у «Дверные откосы» и
  «Лифтовые откосы» объём есть только в справочнике; на тот момент из 137 работ
  46 отсутствовали в поэтажке, значение в справочнике было у 2 из них).
- **Объемы**: то же дерево; после названия — «Ед. изм» и галочка «Лид.» (лидерный объём),
  дальше К1–К12 (узкие колонки 53px, шрифт чисел 11px, название забирает остаток ширины)
  и «Итого» справа. В строке группы работ — сумма по каждому корпусу и итог (только работы
  с признаком лидерного объёма). Клик по работе — модальное окно с поэтажной ведомостью.
  (всё — просьбы пользователя 2026-08-04)
- **Бюджет** (2026-08-04): свод по «Поэтажка_работы» — статья бюджета («Признак для
  бюджета основной», кол. L) × сумма «Стоимость материалы + работа для бюджета» (кол. AG),
  строка «Итого» внизу. Иерархия: клик по статье раскрывает работы, сгруппированные
  по «Группа работ» (кол. F поэтажки; заголовки групп сворачиваются, порядок — по сумме);
  столбцы таблицы: Статья/название · Объем · Коэф. перед. · Стоимость (объём — кол. D
  с ед. изм. из справочника «Работы», коэф. переделки — кол. Q; заполняются на уровне
  работ; 2026-08-05);
  клик по работе — ВЫПАДАЮЩАЯ сводная «подрядчики × корпуса» под строкой (не модалка —
  просьба 2026-08-05): в ячейках стоимость итого из кол. AG, расценки за ед. из сводки
  floors; подрядчик с разными расценками разбит на отдельные строки — по строке на
  комбинацию «раб/ед + мат/ед», чтобы было видно, на каком корпусе какая расценка;
  клик по сумме в ячейке — модальное окно с расшифровкой по этажам (этаж, объём,
  расценки за работу/материалы, стоимость; action=budgetFloors, кэш `bfloors_v1`
  чанкованный, грузится при первом клике; 2026-08-05);
  **столбцы «Работы, руб» и «Материалы, руб»** (2026-08-18) — в основной таблице
  бюджета между «Коэф. перед.» и «Стоимость», на всех уровнях (статья/группа/работа)
  и в «Итого»; работы — сумма cell[5] (AE) с учётом фильтров, материалы — разница;
  без cell[5] в кэше колонки не показываются (`hasSplit`/`splitTd`);
  **колонки факта** (2026-08-21) — справа от «Стоимость»: «Факт работы, руб»
  (объём к закрытию Z × расценка AB, cell[7]), «Факт материалы, руб» (Z × AC,
  cell[8]) и итоговый «Факт, руб» («К оплате», кол. AA, cell[6]); проверено:
  Z×AB + Z×AC = AA с точностью до рубля. На всех уровнях и в «Итого», учитывают
  оба фильтра; со старым кэшем колонки не показываются (`hasFact`/`hasFactSplit`,
  как `hasSplit`); пустой факт — пустая ячейка, не «0»; блок факта объединён
  общей шапкой **«Модель факт»** (просьба 21.08) — шапка таблицы в два ряда
  (подписи: Работы / Материалы / Итого), второй ряд липкий со сдвигом на высоту
  первого (`alignBudgetHead`, пересчёт при смене масштаба из `applyZoom`);
  **колонка «Выполнено (ЛК), руб»** (2026-08-21) — из листа «Личные_кабинеты»
  (МОЛ × статья бюджета → выполнено итого; в заголовке листа опечатка «Статья
  бюждет» — код ищет именно её). Значения только в строках СТАТЕЙ и в «Итого»
  (по работам ЛК не разложить); фильтр по подрядчикам сопоставляет МОЛ с именем
  подрядчика (все 4 МОЛ совпадают с подрядчиками поэтажки, проверено 21.08);
  фильтр по работе на колонку не влияет. Бэкенд отдаёт лист в ответе budget
  (`lk`: [[МОЛ, статья, выполнено], …], `readLk_`); нет листа — колонки нет.
  Статьи ЛК, не совпадающие со статьями бюджета дословно (на 21.08 — 6 из 17:
  Паркинг-статьи, «СБ3 - Работы», «Стены и перегородки», ~20,3 млн), в колонку
  и «Итого» НЕ попадают — **решение пользователя 2026-08-21: работаем только
  с полным совпадением названий, ничего не привязываем**; заново не предлагать;
  **статья «Паркинг»** (2026-08-24) — из листа «Паркинг» (ведомость отделки
  паркинга из экселя «Доп работы по СБ3 от 25.05.2026», перенесена пользователем;
  цепочка подготовки — CSV `Приложения/Паркинг_работы_v3.csv`; **готов v4**:
  `Приложения/Паркинг_работы_v4.csv` (25.08) — колонка «Договор»
  перераспределена по блоку «Бюджет» экселя «Бюджет МОП типовых этажей итог
  от 06.08.2026» (вкладка Паркинг): итог 550 160 011,67 вместо 552,17 млн,
  велком-зоны и ПУИ получили договор, добавлены 16 работ бюджета, которых нет
  в расчёте с заказчиком (помечены в «Комментарий»); **вставлен пользователем
  25.08, сверка после вставки пройдена** — в листе 266 строк;
  сверка и сборка — сессия 25.08). Сумма статьи —
  с 25.08 колонка **«Стоимость итого в бюджет»** (кол. Q = договор ×
  «Коэффициент запаса», колонки P и Q добавлены пользователем в лист;
  нет колонки — фолбэк на «Договор», как было). `readPark_` отдаёт статьи в формате buildBudget_ и
  подмешивается в ответ budget (`.concat`); иерархия групп (финал 25.08 после
  двух итераций): группа = «Раздел» (тип помещения — автостоянка, кладовые,
  МОП и т.д., 9 групп), внутри — ВЛОЖЕННЫЕ подгруппы-поверхности (Полы/Стены/
  Потолок из колонки «Группа работ», бэкенд отдаёт в w[4] только у паркинга;
  фронт рисует строки `tr.bsub` со сворачиванием, `state.budgetSubOpen`,
  работы под ними — класс `bindent`; у статей без w[4] всё как раньше).
  **Группы и подгруппы всех статей по умолчанию СВЕРНУТЫ** (просьба 25.08:
  при раскрытии статьи виден только список групп с суммами; раскрытие кликом,
  фильтр по работе раскрывает сам; `budgetGrpOpen`/`budgetSubOpen` хранят
  раскрытые ключи).
  **Секции статей «Квартиры» и «МОП»** (просьба 25.08) — верхний уровень над
  статьями: «Квартиры» = статьи с «MR Base» в названии, «МОП» = статьи
  «СБ3 - МОП - …» (классификация по названию, `sectOf` на фронте); остальные
  статьи (Паркинг, Лобби, СС Шамова, НР) — отдельными строками. Секции
  сворачиваются (по умолчанию свернуты, `budgetSectOpen`, строки `tr.bsect`
  жирные с тёмным фоном), суммы секций по всем колонкам вкл. ЛК/КП; фильтры
  действуют, пустая по фильтрам секция скрывается; новая статья с MR Base
  или «- МОП -» попадёт в секцию сама;
  ⚠️ плюс ЗАШИТАЯ в `readPark_` группа «Не учтенные работы в расчете с
  заказчиком» — 5 работ на 34 212 885 руб (навигация, двери/шлагбаум, двери
  компакторной, лифтовые порталы; данные пользователя из экселя «Доп работы
  по СБ3», 25.08). В листе «Паркинг» этих строк НЕТ — суммы изменятся, править
  список `extras` в коде; вся сумма в «Работы, руб»,
  работы агрегируются по ключу группа+работа (невидимый символ код 1 — Edit его
  не находит); **переделки паркинга — плановые 10%** (просьба 26.08): каждой
  строке readPark_ ставит коэф. 0,1 (cell[4]) и сумму 10% от стоимости
  (cell[14]) — статья попадает в блок «Переделки» (~63,9 млн), кэш поднят
  до `budget_v28`; работы паркинга НЕ раскрываются по клику (класс `bflat` —
  корпусной разбивки нет, просьба 24.08); ячейка одна на работу: подрядчик «— без подрядчика —», корпус
  «Паркинг», в cell[5] — договорная часть работ (пропорция СМР/всего ведомости),
  факт-колонки пустые. С v4 (25.08) договор есть у ВСЕХ разделов, включая
  ПУИ (4,3 млн) и велком-зоны (36,9 млн) — старое правило «ПУИ и велком без
  договора» больше не действует; статья с неучтёнными = 584,4 млн (сверено
  с экселем от 06.08 по всем разделам до рубля, расхождение статьи 2 руб —
  округление работ). Числа листа парсятся и строками с запятой (`parkNum_`).
  **статья «Лобби»** (2026-08-25) — из листа «Лобби» (нормализованные тендерные
  бюджеты отделки лобби из того же экселя от 06.08: вкладки МПСИ (К5, К7–К10),
  ВЕЛМИ (К1, К2, К4, К6, К11, К12 — блоками) и «Авангард лобби» (по покрытию
  корпусов = К3); цепочка подготовки — CSV `Приложения/Лобби_работы_v2.csv`
  (актуален v2, вставлен 25.08; v1 остался в папке историей), парсер
  восстанавливал дерево агрегатов: маркеры «№ раздела»/«Статья СМР» +
  числовое доказательство сумм). Итог **372 346 691,15** — до копейки со
  сводником пользователя (сумма = «Стоимость всего», тендерный блок УСН НДС
  22%); `readLobby_`: группы = зоны «Лобби К1…К12» + группа «Не учтенные
  работы в расчете с заказчиком» (30 236 226: стеклянные полки, инд. изделия,
  сантехника — суммы из сводника, в тендерах эти позиции с нулями),
  подгруппы w[4] = «Группа работ» (Полы/Стены/Потолок/ДВЕРИ/Люки/Сантехника…),
  подрядчик ячейки = МПСИ-2/ВЕЛМИ/СК-Авангард, корпус 'Лобби' (строки плоские,
  bflat); cell[5] «Работы» = СМР + косвенные. Сверки 25.08: все вкладки до
  рубля; ⚠️ **К9 взят по блоку «КП СО СКИДКОЙ»** (22 885 170 — так в своднике;
  решение пользователя 25.08; в ОСНОВНОМ блоке К9 к тому же ошибка экселя —
  итог завышен на 971 766 двойным счётом дверей); ~425 строк с нулевой
  стоимостью (светильники/сантехника в отдельном бюджете) оставлены справочно;
  **статья «Собственные силы факт на 01.08.2026 (табели и премии)
  почасовщики»** (2026-08-25) — 150 000 000 руб (уточнение пользователя
  25.08; исходно из сводника было 228 908 513 с пометкой «Уточняется»),
  построчных данных нет — ЗАШИТА
  в `readSsFact_` (изменится — править SS_FACT_SUM). Одна плоская работа
  (corp `'СС факт'` в списке bflat), сумма в «Работы»; подрядчик не указан —
  при активных фильтрах по подрядчикам/галочке СС статья скрывается.
  План Шамова остаётся отдельной статьёй;
  **статья «Подсоба Шамов»** (2026-08-24; до 25.08 называлась «Собственные
  силы (Шамов)») — из листа «расчет_Шамов»:
  `readShamov_`. Иерархия (финал 24.08 после двух итераций): группы = **разделы
  листа как были**, но с 25.08 разделы **«Общестрой» и «Тепловой контур»
  ИСКЛЮЧЕНЫ из статьи** (строки пропускаются, просьба 25.08 — остаются
  Отделка/Паркинг/Сдельщики, статья ~303,3 млн вместо 367,5),
  строки работ = **ВИДЫ работ** (`shamovKind_` отрезает перечень корпусов от
  названия; «Бригадир/Бригадиры» склеены, опечатка «выполненых» исправляется;
  у сдельщиков вид = полное название), клик по виду — **своя сводная**
  (`budgetSSHtml`): строки = позиция (полное название с корпусами, cell[13]) ×
  МОЛ, колонки Позиция · МОЛ · Месяцев (cell[11]) · Ср. людей (cell[12]) ·
  Чел.-часы (cell[9], решение 24.08 — именно часы, не дни) · Ставка ₽/час
  (cell[10]) · Стоимость, без корпусных колонок. МОЛ = «подрядчик» ячейки,
  корпус 'СС'; у сдельщиков — «— без подрядчика —»; вся сумма в cell[5]
  «Работы» — трудозатраты; итог 367,5 млн. Кэш `budget_v16`;
  В «Форме КП» паркинг-статьи («Паркинг - МОП - …») с 24.08 считаются в строку
  «Паркинг» колонки «Закрытие у заказчика» (правило на фронте: статья КП,
  начинающаяся с «паркинг», → «Паркинг»; ~176,8 млн). В ЛК паркинг-статьи
  по-прежнему НЕ увязываются (полное совпадение, решение 21.08);
  **статья «Накладные расходы»** (2026-08-25) — суммы кол. AH поэтажки в разрезе
  «Подрядчик сводный» (кол. S), считает бэкенд (`readOverhead_`, подмешивается
  `.concat` последней). «Работы» статьи — подрядчики (пустой → «Не определен»,
  ключ ячейки остаётся «— без подрядчика —» для фильтра), группа одна —
  «Подрядчики»; строки плоские, не раскрываются (корпус ячейки `'НР'`, тот же
  механизм `bflat`, что у Паркинга); сумма и в cell[5] «Работы, руб» (как у
  Шамова). Статья всегда В САМОМ НИЗУ списка статей (`isBottomItem` в сортировке,
  просьба 25.08). Входит в «Итого», НО при галочке «Собственные силы»
  ИСКЛЮЧАЕТСЯ и из таблицы, и из «Итого» (`rowsT` в renderBudget — просьба
  25.08: бюджет собственных сил без накладных). С 26.08 НР фиксируется в базе
  вместе с остальными доп-статьями (extras) — «Базовый бюджет» по статье
  заполняется после перефиксации базы;
  **блок «Переделки»** (2026-08-25, вторая итерация в тот же день) — «Коэф.
  перед.» и «Сумма переделок, руб» вынесены В ПРАВЫЙ КРАЙ таблицы (после
  «Закрытия у заказчика») под общую шапку «Переделки» (терракотовый тон
  `.redohead`/`.h-redo`, механика как у «Модели факт»); из-за этого второй ряд
  шапки существует всегда и rowspan=2 у остальных колонок безусловный.
  Сумма переделок считается бэкендом по строкам поэтажки: стоимость с коэффициентом
  (AG) минус «Стоимость без коэф» (новая кол. AI) — **выбор пользователя 25.08
  из двух трактовок** (в запросе фигурировал «столбец AJ», но AJ в листе —
  «Факт ИИ админка»; привязка в коде — по ИМЕНИ заголовка AI, не по букве).
  Идёт в cell[14] (индексы 9–13 заняты спец-сводной Шамова); значения на всех
  уровнях + «Итого», оба фильтра действуют; старый кэш без cell[14] — колонки
  нет (`hasRedo`, как `hasSplit`);
  **колонка «Базовый бюджет, руб»** (2026-08-25, выбор пользователя из двух
  трактовок: не «по базовым расценкам», а из зафиксированной базы) — суммы из
  слепка базового расчёта (тот же `otdelka_baseline.json`, что у проверки
  «Изменения»; в подсказке шапки — дата фиксации). Бэкенд сворачивает слепок
  в `base: {date, works: [[работа, подрядчик, сумма], …]}` (`readBaseSums_`,
  кэш поднят до `budget_v17`; фиксация новой базы сбрасывает и кэш бюджета);
  фронт раскладывает по работам бюджета по названию (`baseByWork`/`wSumB`),
  значения на всех уровнях + «Итого», колонка сразу после «Стоимость»
  (шапка сиреневая `.h-base`). ⚠️ **Первая колонка бюджета — фиксированные
  340px** (26.08): при width:auto колонка получала «остаток» контейнера, а
  колонок стало ~14×110px — при обычном окне остаток нулевой и названия статей
  «пропадали» (два ложных расследования: bfilter-dd и кэш браузера). Не
  возвращать auto. Оба фильтра действуют; фильтр по подрядчикам —
  по подрядчику, каким он был В БАЗЕ (несколько на этаже склеены « + » —
  совпадение по любому). **Доп-статьи, которых нет в поэтажке** (Паркинг, Лобби,
  Подсоба Шамов, СС факт, Накладные расходы), **с 2026-08-26 тоже фиксируются**
  при saveBaseline — отдельным списком `extras` в `otdelka_baseline.json`
  (`buildBaselineExtras_`: [[статья, группа, подгруппа, работа, подрядчики,
  сумма], …]; ключ на фронте — вся цепочка уровней `bexKey`, по одному названию
  работы нельзя — имена совпадают с работами поэтажки и суммы задваивались бы).
  В базе, зафиксированной до 26.08, extras нет — ячейки доп-статей пустые до
  перефиксации. Работа, которая была в базе, но ушла из текущего бюджета
  (AG обнулился), в колонку и «Итого» не попадает. База не зафиксирована /
  старый кэш — колонки нет (`hasBase`, как `hasSplit`);
  **колонка «Закрытие у заказчика, руб»** (2026-08-24) — из листа «Форма КП»:
  статья бюджета (кол. N «Признак бюджет деньги основной») → сумма закрытия
  (кол. F «Выполнено с начала строительства стоимость руб.»), сворачивает бэкенд
  (`readKp_`, в ответе budget поле `kp`). Как и ЛК: значения только в строках
  статей и «Итого», только полное совпадение названий (несовпавшие на 24.08 —
  Паркинг-статьи, ~176,8 млн, в бюджет отделки не входят). Разреза по
  подрядчикам/работам в «Форме КП» нет — фильтры на колонку НЕ влияют;
  **раздельные суммы в ячейках сводной** (2026-08-18): сверху — работы (кол. AE,
  бэкенд отдаёт в cell[5], кэш `budget_v8`), снизу приглушённо — материалы
  (итог AG − работы AE), легенда под таблицей, подсказка при наведении; старый
  кэш без cell[5] показывается одной суммой (fallback `hasSplit`);
  **фильтр по работе** (2026-08-18): поле «Фильтр по работе...» рядом с фильтром
  подрядчиков (`#bsearch`, `state.budgetQuery`) — совпавшие работы показываются
  с автоматически раскрытыми статьями/группами, суммы статей/групп и «Итого»
  пересчитываются по совпавшим; комбинируется с фильтром подрядчиков;
  **галочка «Собственные силы»** (2026-08-18): рядом с фильтрами (`#bss-check`,
  `state.budgetSS`) — добавляет к фильтру СС-четвёрку из зашитого списка
  `BUDGET_SS_CONTRS` (Джамал, Овчинников, Элшод, Смирнов; имена как в поэтажке,
  новый СС-подрядчик — дописать в список); действующий набор = ручной выбор ∪
  СС-четвёрка (`budgetSelSet()`), действует и в раскрытой сводной;
  два блока с заголовками — «Подрядчики» и «Собственные силы» (признак СС из floors);
  колонка называется «Название»; сверху строка «Не определен» (строки поэтажки без
  подрядчика, расценки — базовые из справочника «Работы»); внизу сумматор «Итого»
  по корпусам, справа колонка «Итого» по строке; можно раскрыть несколько работ сразу
  (`budgetWorkOpen` — Set); весь выпадающий блок залит синим цветом выделения, без
  цветовой индикации строк внутри, без заголовка и подписи (2026-08-05). Бэкенд отдаёт
  `[[статья, сумма, [[работа, сумма, [[подрядчик, корпус, сумма], …]], …]], …]`,
  кэш `budget_v3` чанкованный. ⚠️ В `buildBudget_` ключ подрядчик+корпус склеен через
  НЕВИДИМЫЙ символ (код 1, как в `buildFloorSummary_`) — Edit по этим строкам не находит
  текст, править кусками вокруг. Считает бэкенд (action=budget, кэш 6 ч), фронт грузит лениво.
  **Фильтр по подрядчикам** (2026-08-12): над таблицей кнопка «Подрядчики: все» →
  выпадающий список с галочками (мультивыбор; «Не определен» — строки без подрядчика —
  первым, дальше по алфавиту; внизу «Сбросить выбор»). При выборе суммы статей, групп
  и работ (а также объём и коэф. переделки в строке работы) пересчитываются только по
  выбранным подрядчикам; статьи/работы, где у выбранных ничего нет, скрываются; «Итого»
  тоже по фильтру. Фильтр действует и в раскрытой сводной «подрядчики × корпуса»
  (остаются только строки выбранных). Считается целиком на странице (`state.budgetContr`
  Set + `budgetContrOpen` в `renderBudget`/`budgetWorkHtml`) — бэкенд не менялся; пустой
  выбор = показывать всех. Задача пользователя: видеть, сколько денег заложено на
  конкретного подрядчика в разбивке бюджета.
- **Проверки**: «Недостающие расценки» (готовый лист «Проверки расценки»), «Отклонения
  расценок» (базовая vs средние по подрядчикам/СС, порог 15%), «Без стоимости»
  (работы справочника «Работы» с пустой/нулевой баз. расценкой работ или материалов;
  только работы с «Объем работ» > 0 — уточнение 2026-08-05; считается на клиенте —
  `noCostChecks`; сгруппирована по «Группа работ», по умолчанию всё свернуто;
  добавлена 2026-08-04), «Формулы» (работы с ошибками формул #N/A/#REF!/… в строке
  листа «Работы» или в материалах листа «Расходы» — ошибки приходят в выгрузке текстом;
  `formulaChecks`, на клиенте; добавлена 2026-08-05), «Изменения» (дифф текущей
  поэтажки против БАЗОВОГО РАСЧЁТА — слепка «работа|корпус|этаж → [стоимость AG,
  объём D, подрядчик, расц. AB, расц. AC]»; слепок хранится JSON-файлом
  `otdelka_baseline.json` на Диске владельца, ID в Script Properties BASELINE_FILE_ID;
  кнопка «Зафиксировать…» на экране перезаписывает базу через POST saveBaseline
  и сбрасывает кэш `changes_v1`; с 26.08 фиксация дополнительно сохраняет в файл
  слепок доп-статей бюджета (`extras`, см. колонку «Базовый бюджет» выше) —
  в дифф «Изменений» extras не участвуют; сравнение — action=changes, считает бэкенд;
  ⚠️ **фиксация базы — только для администратора** (2026-08-18): saveBaseline
  требует пароль `at`, бэк сверяет со Script Properties `ADMIN_PASSWORD`
  (значение — в ДОСТУПЫ.md; свойство задаётся вручную в редакторе GAS, пока
  не задано — фиксация закрыта для всех). Фронт спрашивает пароль при первом
  клике и держит в localStorage `otdelka_admin`, при неверном — стирает;
  добавлена 2026-08-05. **С 2026-08-14 изменения сгруппированы как во вкладке
  «Бюджет»**: статья бюджета → группа работ → работа (все уровни сворачиваются,
  статьи по алфавиту, группы/работы — крупные сдвиги стоимости сверху), у каждого
  уровня — число изменений и «Δ стоимости» против базы; клик по работе раскрывает
  строки корпус × этаж, детали — отдельными столбцами (Подрядчик · Стоимость
  материалов за ед. · Стоимость работ за ед. · Объём · Стоимость, «было → стало»
  красным). Соответствие «работа → статья/группа» берётся
  из свода бюджета (action=budget) на странице, бэкенд не менялся; если свод
  не загрузился — плоский список, как раньше; работы, которых нет в бюджете, —
  в статье «— вне бюджета —») и «Вопросы» + бейджи-счётчики.
  (переименованы 2026-08-04; имена экранов в коде прежние: check-rates / check-work-rates)
- **Вопросы по работам** (2026-08-04, первая функция с записью): кнопка «Вопрос» у каждой
  работы на «Расценках» → модальное окно (имя + текст) → POST на бэкенд. Хранение —
  **JSON-файл `otdelka_questions.json` на Google Drive владельца скрипта** (выбор
  пользователя 2026-08-04, Google-таблица не используется). Статусы «Открыт»/«Закрыт»
  переключаются на экране «Вопросы»; при создании и смене статуса фиксируются автор
  и время. Имя пользователя хранится в localStorage (`otdelka_user`) и подставляется само.
  ⚠️ **Разовая авторизация**: доступ к Диску — новый scope; после первого деплоя владелец
  (kuzkin@acons.group) должен один раз запустить `setupQuestions()` в редакторе GAS
  и принять права, до этого вопросы не работают (экран покажет подсказку).
- **Факт** (2026-08-10): отметка фактически выполненных работ **поэтажно** — процент
  готовности на ячейку корпус × этаж, кнопка ✓ = 100% (выбор пользователя 2026-08-10:
  процент + галочка, разрез поэтажный, хранение — отдельный лист таблицы). То же дерево,
  что «Объемы»; в колонках К1–К12 — средневзвешенный % готовности работы по корпусу
  (веса — объёмы этажей из vols), в строках групп — только по лидерным работам.
  Клик по работе — модальная сетка этажи × корпуса: план-объём, поле процента, ✓ ячейки,
  «✓ все» на корпус (с подтверждением); строка «Готовность» внизу. Сохранение —
  optimistic UI: очередь с паузой 1,2 с, пакет одним POST `saveFact`, 3 ретрая, при
  ошибке откат значений + alert; при закрытии модалки несохранённое уходит сразу;
  `beforeunload`-предупреждение. Имя автора — то же, что у вопросов (`otdelka_user`).
  **Хранение — лист «Факт» в «Отделка ИИ», ЖУРНАЛОМ**: строки только добавляются
  (Дата·Кто·Работа·Корпус·Этаж·Процент), последняя отметка по ключу работа|корпус|этаж —
  актуальная; существующие строки/листы скрипт не трогает. ⚠️ В поэтажку писать нельзя —
  она собрана IMPORTRANGE, запись затрётся формулами. Справочно в подсказках ячеек —
  факт из поэтажки: «Процент готовности» (V) и «Объем к закрытию» (Z), action=factRef
  (на 2026-08-10 «К оплате»/Z заполнены лишь у ~4 тыс. строк из 25 тыс. — поэтому и
  понадобилась своя отметка). Ввод экранируется `safeCell_()` на бэке.
  Актуальное значение по ячейке = последняя запись журнала по ключу (снизу вверх).
  Свод-лист «Факт_свод» для увязки с основной таблицей предлагался и **отклонён**
  (решение пользователя 2026-08-10: «увяжусь сам по-другому») — заново не предлагать.
- Галочка «Лидерный объем» — бывший «Ген. объем», переименована по просьбе пользователя
  2026-08-04 (признак тот же: «Признак для бюджета дополнительный объем»).
- Связка работа ↔ материалы: по `Название работы из АР`, сравнение после `trim()`
  в нижнем регистре (`norm()`).

Редактирование данных через панель — отдельная задача на будущее, пока не реализовано.

**Состояние**: развёрнуто, работает. Бэкенд — deployment @59 (2026-08-26).
Инцидент 26–27.08: публикации GitHub Pages падали («Page build failed») —
сборщик Jekyll сбоил на стороне GitHub. Решение 27.08: в репозиторий добавлен
пустой файл `.nojekyll` (Jekyll для статичного index.html не нужен, публикация
идёт напрямую) — сборка прошла, прод содержит свежий код ТУЗИО (`sumSrc(a.tuzio)`).
Файл `.nojekyll` не удалять.

## Подключения

| Сущность | Значение |
|---|---|
| Spreadsheet ID (источник данных) | `1-Fd0eTinA8jiJ6UM5jazzj8vfW0wRceZPT7eKOSkutA` («Отделка ИИ») |
| Apps Script ID | `1o3TF5Sy4WO3CpTGsac-qLeyoE0xWZe9mMQBpHDgC29CQKhywvxnBx9fW` |
| GAS Web App URL (production) | `https://script.google.com/macros/s/AKfycbyo6f-OGca1loM1aBXtHkcx7nq8tlJWBreGepxp0KTwq7GvDdDPKo-tuFjXQqKs1nZfdQ/exec` |
| Production deployment ID | `AKfycbyo6f-OGca1loM1aBXtHkcx7nq8tlJWBreGepxp0KTwq7GvDdDPKo-tuFjXQqKs1nZfdQ` |
| GAS аккаунт (clasp) | `kuzkin@acons.group` |
| GitHub репо | `https://github.com/Nick3000ept/otdelka` |
| GitHub Pages URL | `https://nick3000ept.github.io/otdelka/` |
| Пароль входа в витрину | `otdelka2026` (хранится в Script Properties, ключ `PASSWORD`, НЕ в коде) |

### Про источник данных

«Отделка ИИ» (`1-Fd0eTinA8jiJ6UM5jazzj8vfW0wRceZPT7eKOSkutA`) — небольшая таблица-выгрузка, которую пользователь собрал сам через `IMPORTRANGE` из исходной боевой таблицы «Отделка СБ3 база» (`18vXrLIZ7QRqyB54i4ZZzFFgfKM1L5bSchOaWQ8UF3cg`, владелец `kuzkin@acons.group`, ~12 МБ, десятки вкладок с пивотами и историей факта). Бэкенд читает **только** «Отделка ИИ» — с боевой таблицей напрямую не работает и не должен (решение пользователя от 2026-08-02: «Отделку СБ3 база забудь»).

## Листы в «Отделка ИИ»

Состав на 2026-08-03 (проверено через `?action=meta`). Бэкенд читает **все листы, кроме
`Импорт` и `Форма КП`** — пометки «в витрине» у каждой строки.

| Лист | Размер | Назначение |
|---|---|---|
| `Импорт` | 27×3 | Служебный — таблица ссылок `IMPORTRANGE` на первоисточники. Не читается бэкендом. |
| `Работы` | 1139×17 | **В витрине.** Название работы из АР, ед. изм., группа, место, поверхность, локация, название работы СС, ИД факт, признаки для бюджета, «Базовая расценка работа в расчет, руб», плановый процент переделки, объём, стоимость материалов базовая за ед. |
| `Материалы` | 273×6 | **В витрине** — только как справочник ед. изм. материала. Материал, ед. изм., расход на ед. работы, комментарий, тип материала, «Базовая цена материалов в расчет». |
| `Расходы` | 1010×13 | **В витрине** — раскрывается под работой. Тех.лист: работа → материал → расход на единицу (место, тип работ, слои, ручной/автоматический расход, расценка, стоимость материала в ед. работы, объёмы). |
| `Поэтажка_работы` | **24999×36** | **В витрине** — только как источник сводки «подрядчик × корпус» (`action=floors`, наружу идёт агрегат). Работы в разрезе корпус → этаж: объём, подрядчик (сводный / договор / шахматка), процент готовности, расценки СС и СП, объём к закрытию, к оплате, полный бюджетный блок + накладные расходы (AH). С 2026-08-25 добавлены AI «Стоимость без коэф» (для «Суммы переделок» = AG − AI) и AJ «Факт ИИ админка» (витриной не используется). |
| `Расценки подрядчиков` | 810×9 | **В витрине** — детали по клику на подрядчика. Стоимость работ и материалов за единицу по подрядчику с областью действия: корпус, этаж начало/конец, этажи включить/исключить. |
| `Расценки СС` | 98×6 | **В витрине** — детали по клику на СС-подрядчика. Свод по подрядчику и работе: на руки, с налогами, накладные (подсоба, бригадиры, патенты, общежития, ПОС, билеты), итого. |
| `Справочник СС` | 152×7 | **В витрине** — мэппинг СС. Соответствие названий: работа СС ↔ работа для расценок модель отделка АР ↔ работа факт админка ↔ статья бюджета АИ. |
| `Проверки расценки` | 40×6 | **В витрине** — экран «Проверка расценки» как есть. Подрядчик шахматка / Группа работ / Корпус / Работа / MIN и MAX из Этаж. Собирается самой таблицей. |
| `Форма КП` | 4994×56 | **В витрине с 2026-08-24** — свод «статья (N) → закрытие у заказчика (F)» для колонки на «Бюджете» (`readKp_`, наружу идёт агрегат). С 2026-08-26 также месячные колонки закрытия (с кол. X, даты месяцев в строке 1) — линия «поступления» на вкладке «Аналитика» (`buildAnalytics_`). Отчётная форма: кол-во в ВДЦ, выполнение с начала строительства (объём, руб., %), коэффициенты, уровни 1–3, итоги за материалы и работы с НДС 20%. ⚠️ Две даты-заголовка с опечаткой года и одна колонка без даты — см. вкладку «Аналитика» ниже. |
| `МОРС` | 9881×11 | **В витрине** (2026-08-26) — линия «затраты» на вкладке «Аналитика». Журнал платежей всей стройки: Контрагент · Договор · Назначение · Тип платежа · «Сумма расход» · Статус (на 26.08 всё «Оплачено») · «Дата проводки» · Очередь · «Статья бюджета» (79 статей, включая фасады/монолит/инженерку) · Описание. Наружу идёт только агрегат статья × месяц. |
| `Факт` | растёт | **Создаётся скриптом** (2026-08-10) — журнал отметок факта с витрины: Дата · Кто · Работа · Корпус · Этаж · Процент. Строки только добавляются (append-only), последняя отметка по ключу работа|корпус|этаж — актуальная. Единственный лист, куда скрипт ПИШЕТ. |
| `Личные_кабинеты` | 35×3 | **В витрине** (2026-08-21) — колонка «Выполнено (ЛК)» на «Бюджете». МОЛ · Статья бюждет (опечатка в листе) · Выполнено итого. |
| `Паркинг` | 266×17 | **В витрине** (2026-08-24) — статья «Паркинг» на «Бюджете» (сумма = кол. Q «Стоимость итого в бюджет» = договор × «Коэффициент запаса» 1,1; колонки P/Q добавлены пользователем 25.08). Ведомость отделки паркинга: статья · раздел · группа · работа · этаж (−3/−2/−1) · объём · цены · стоимости · договор · коэф. запаса · стоимость в бюджет. С 2026-08-25 — из CSV `Приложения/Паркинг_работы_v4.csv` (договор перераспределён по экселю «Бюджет МОП типовых этажей итог от 06.08.2026», итог договора 550 160 011,67; 16 работ только из бюджета помечены в «Комментарий»), вставлен пользователем, 266 строк сверены (⚠️ `meta` считает строки БЕЗ заголовка — не путать при сверках). |
| `Лобби` | 1574×16 | **В витрине** (2026-08-25) — статья «Лобби» на «Бюджете» (сумма = «Стоимость всего», 372 346 691 — до копейки со сводником пользователя). Нормализованные тендерные бюджеты отделки лобби из экселя «Бюджет МОП типовых этажей итог от 06.08.2026»: зона (Лобби К1…К12 + «Не учтенные работы в расчете с заказчиком») · группа (Полы/Стены/…) · подрядчик (МПСИ-2/ВЕЛМИ/СК-Авангард) · работа · цены и стоимости (мат/СМР/косвенные/всего, УСН НДС 22%). Вставлен пользователем из CSV `Приложения/Лобби_работы_v2.csv` (v2 25.08: К9 по блоку «КП со скидкой», + неучтённые 30,2 млн), сверка после вставки пройдена. |
| `Списание материалов` | 200×6 | **В витрине с 28.08.2026** — вкладка «Материалы» (`action=writeoff`, `readWriteoff_`, кэш `writeoff_v1` 6 ч, лист маленький — обычный кэш, не чанкованный). Тип материала · Материал · Ед. изм · «Объем материала итого» · «Материал к списанию итого» · МОЛ. Структура: 50 материалов × 4 МОЛ (Овчинников, Джамал, Элшод, Смирнов) — у всех МОЛ ОДИН И ТОТ ЖЕ список материалов, ровно по 50 строк. Денег/цен нет, только натуральные объёмы (цена материала — на листе `Материалы`, кол. F). Итоги на 27.08: объём 8 209 664,92, к списанию 4 972 882,49. ⚠️ Нюансы: «Тип материала» пуст у 116 строк из 200 (группировка по типу покроет меньше половины); 43 строки, где «к списанию» БОЛЬШЕ «объёма» (у Элшода это видно даже в итоге: 13,9 тыс. против 30,8 тыс.); много нулей (118 в объёме, 97 в списании); значения могут прийти текстом с десятичной запятой — `readWriteoff_` их парсит. ✔ Проверено 28.08: тип проставлен ровно у 20 материалов и ОДИНАКОВО у всех 4 МОЛ (80 строк), поэтому группировка по типу ничего не теряет. |
| `ТУЗИО_часы` | 58871×7 | **Создан скриптом** (2026-08-31, doPost `importTuzio`) — почасовые начисления рабочим для вкладки «ТУЗИО». Месяц («ГГГГ-ММ» текстом) · Статья бюджета · Табель № · День · ФИО · Часы · Сумма. Строка = «человек в одном табеле за один день». Источник — эксель `Контроль_договоров/Приложения/Часы 2026.xlsx`, отбор и раскладка — скрипт `Приложения/tuzio_import.py`. В `load` НЕ входит (лист большой), наружу идёт только агрегат `action=tuzio`. Обновление — повторный запуск скрипта (перезаписывает лист целиком) + «Обновить данные». |
| `расчет_Шамов` | 419×9 | **Создан скриптом** (2026-08-24, doPost `importShamov`) — плоский бюджет собственных сил из экселя «Бюджет собственных сил Шамов» (вкладка СБ3): Раздел (Отделка/Паркинг/Сдельщики/Тепловой контур/Общестрой) · МОЛ · Месяц (текстом «2026-08») · Работа · Роль · Кол-во · Ставка ₽/час · Часов в месяц · Сумма (итого 367,5 млн; расхождения с итогами экселя ~1,2 млн — нестыковки разбивки по работам в самом файле). **В витрине с 24.08** — статья «Собственные силы (Шамов)» на «Бюджете» (`readShamov_`). Обновление — повторный POST importShamov (перезаписывает целиком, только этот лист) + сброс кэша. |

Смысловая цепочка: `Работы`/`Материалы`/`Расходы` — нормативы на единицу; `Поэтажка_работы` —
те же нормативы × реальные объёмы по этажам; `Расценки подрядчиков`/`Расценки СС` — сколько
платим конкретному подрядчику; `Справочник СС` сшивает разные названия одной работы;
`Форма КП` — выгрузка наружу.

> ⚠️ **`Поэтажка_работы` нельзя просто добавить в `load`** — 25 тыс. строк × 34 колонки
> раздуют ответ в 10–20 раз (десятки МБ): GAS упрётся в лимит времени выполнения, фронт
> не отрисует. Понадобится — отдавать отдельным action с фильтром по корпусу/этажу или
> постранично, не в общем снапшоте.

Формат отдачи: `doGet?action=load` конвертирует каждый лист в массив объектов «заголовок
столбца → значение» (первая строка листа = ключи). ⚠️ В JSON новая колонка попадёт сама,
но **на странице автоматически уже не появится**: колонки таблицы работ жёстко заданы
в `COL_ORDER`, детали/материалы/проверка обращаются к колонкам по именам. Переименование
колонки в Sheets ломает соответствующий кусок витрины — сверяться с именами в `index.html`
и `script.gs` (CONFIG).

## Деплой

### Фронт (index.html)
```
cd "VS_hub/Отделка"
git add index.html
git commit -m "..."
git push
```
GitHub Pages подхватит через ~1 минуту.

### Бэк (script.gs)
```
cd "VS_hub/Отделка"
clasp push --force
clasp update-deployment AKfycbyo6f-OGca1loM1aBXtHkcx7nq8tlJWBreGepxp0KTwq7GvDdDPKo-tuFjXQqKs1nZfdQ --description "vN: что изменилось"
```
**`update-deployment`, не `deploy`** — иначе новый URL и фронт ломается.

## Вкладки (боковая панель, 2026-08-03)

Слева тёмная панель по образцу КЛАД (`#sidebar`, `state.screen`):
- **Расценки** (`rates`, по умолчанию) — всё описанное ниже: дерево работ с характеристиками,
  нижняя панель с подрядчиками и материалами.
- **Объемы** (`volumes`) — та же иерархия Место→Поверхность→Группа→Работа, но вместо
  характеристик — **12 колонок корпусов** с суммарным объёмом работы по корпусу.
  Клик по работе — **модальное окно с поэтажной ведомостью**: строки этажи, столбцы корпуса,
  внизу «Итого» по корпусу; в подзаголовке ед. изм. работы. Нижней панели на этой вкладке нет.
  Данные — `doGet?action=volumes` (агрегат по поэтажке: Работа/Корпус/Этаж/Объем →
  `{работа: {корпус: [[этаж, объём],…]}}`, 189 КБ, 91 работа; холодный ~30 сек, поэтому
  кэш **кусками** `cachePutBig_`/`cacheGetBig_` (ключи `vols_v1_*`, лимит CacheService
  100 КБ/ключ обходится разбиением; из кэша ~3 сек). Грузится лениво при первом заходе
  на вкладку (`loadVols`), суммы по корпусам предрассчитываются в `state.volsTotals`.
  `clearCache()` чистит и floors, и vols-ключи.
- **Факт** (`fact`, 2026-08-10) — то же дерево, в колонках корпусов — % готовности;
  клик по работе — сетка отметок (см. подробный блок в разделе «Проект» выше).
  Данные: vols (веса) + action=fact (отметки) + action=factRef (справка, фоном).

- **Шапка на всех экранах, кроме «Аналитики»** (2026-08-26): **две строки**
  (`.frow`) — сверху фильтры-списки (Группа работ · Место · Поверхность ·
  Подрядчик), снизу галочка СС, «Сбросить», масштаб А−/100%/А+ и поиск
  (поиск — в ПЕРВОЙ строке справа, масштаб — во второй справа, `.zoom-wrap`;
  финал 26.08 после двух итераций). На «Проверках» класс `header.compact` прячет
  фильтры/СС/«Сбросить»; на «Бюджете» класс `header.bmode` прячет только
  «Место»/«Поверхность» (их нет в данных бюджета), а **«Группа работ»
  фильтрует и бюджет** (grpSel в renderBudget, сравнение с w[3]); «Сбросить»
  очищает и бюджетные фильтры (budgetContr/budgetSS/budgetQuery). В фильтре
  «Подрядчик» пункт **«— не назначено —»** (value `__none__`, 26.08): на дереве —
  работы без единого подрядчика в сводке floors, на «Бюджете» — строки
  «— без подрядчика —». Поиск в шапке контекстный: на дереве —
  прежний поиск по колонкам, на «Бюджете» — тот же фильтр по работе, что
  `#bsearch` (поля синхронизированы), на «Проверках» — фильтр строк таблиц по
  тексту (`state.checkQuery`/`applyCheckSearch`, накладывается заново через
  MutationObserver после перерисовок). Кнопка «Развернуть все» УБРАНА (просьба
  26.08; уровни раскрываются кликом). В шапке также **фильтр «Подрядчик»**
  (`#f-contr`/`state.contrSel`, список из floors после загрузки,
  `populateContrFilter`) и **галочка «Собственные силы»** (`#f-ss`/`state.ssSel`):
  на дереве работ (Расценки/Объемы/Факт) остаются работы, где в поэтажке есть
  выбранный подрядчик ∪ СС-четвёрка при галочке; на «Бюджете» выбор
  транслируется в `budgetContr`/`budgetSS`; на «Проверках» оба скрыты (не
  действуют там). **Собственный блок фильтров бюджета
  (Подрядчики/Фильтр по работе/СС) УБРАН 26.08** — дублировал шапку, его
  выпадающий список перекрывал колонку статей («пропало название статьи»);
  state.budgetContr/budgetSS/budgetQuery остались, управляются шапкой
  (мультивыбор подрядчиков бюджета заменён одиночным выбором из шапки).
  **Список работ у поиска** (26.08): при фокусе/вводе в `#search` выпадает
  список работ текущего экрана с галочками (`#search-dd`,
  `buildSearchDd`/`searchWorkList`, `state.workSel` — Set norm-имён, кап 300
  строк); отмеченные работы фильтруют дерево и «Бюджет» (в renderBudget
  `q`-маркер 'sel' включает автораскрытие и пересчёт сумм), текст в поле при
  активном выборе сужает только список; сброс — строкой сверху списка или
  кнопкой «Сбросить».
- **Аналитика** (`analytics`, 2026-08-26, пункт под «Бюджетом») — накопительный график
  «затраты — поступления» по месяцам, рукописный SVG без библиотек
  (`loadAnalytics`/`renderAnalytics`); вписывается в ширину экрана без прокрутки
  (шаг от ширины контейнера, подписи месяцев прореживаются, перерисовка при resize). Затраты — лист «МОРС» («Сумма расход» по месяцу
  «Даты проводки», разрез «Статья бюджета»); поступления — месячные колонки закрытия
  у заказчика в «Форме КП» (первая — кол. X = `KP_MONTHS_START` 24, даты месяцев
  в строке 1, колонки без даты в заголовке пропускаются; статья — кол. N). Бэкенд —
  `action=analytics` (`buildAnalytics_`, кэш `analytics_v1` 6 ч): месяцы + словари
  статья → {месяц: сумма} по обоим источникам — на экране пока две суммарные линии,
  разрез по статьям в данных уже есть (на будущее). В ответе также `morsStatus` —
  суммы МОРС по «Статусу» (на 26.08 всё «Оплачено», фильтра по статусу нет).
  ⚠️ С 26.08 (вторая итерация) линии считаются ТОЛЬКО по статьям отделки —
  зашитый список `AN_ITEMS` на фронте (из скриншота сводника пользователя;
  сравнение `anMatch` после norm + замена длинного тире, статьи с началом
  «паркинг»/«лобби» — по префиксу; новая статья отделки — дописать в список).
  МОРС целиком — вся стройка (79 статей, 27,6 млрд), после фильтра ~1,09 млрд;
  КП после фильтра ~0,91 млрд. Пустые месяцы до первых движений отрезаются.
  **«ТУЗИО»** (26.08): лист «ТУЗИО» (52×38) — месячные
  колонки с кол. C (даты последних дней месяцев в строке 1,
  `SHEET_TUZIO`/`TUZIO_MONTHS_START`), названия статей — кол. B (в кол. A
  числовые итоги строк). Бэкенд отдаёт `tuzio` в разрезе статья → {месяц:
  сумма}; фронт применяет ТОТ ЖЕ фильтр отделочных статей (anMatch, уточнение
  пользователя 26.08 — только учтённые у нас статьи; проходит 21 строка из 47,
  итог ~420,2 млн) и фильтр выбранной статьи (линия пересчитывается по ней).
  Кэш `analytics_v4`. **С 27.08 ТУЗИО — не отдельная линия**: линия затрат одна,
  накопительно МОРС + ТУЗИО (`cTotal`), под ней двухслойная заливка-полигоны
  (`band`, opacity 0.16) — снизу доля МОРС (красная), сверху доля ТУЗИО (синяя);
  подсказка на точке затрат (`dotsCost`) даёт разбивку месяца на МОРС и ТУЗИО;
  в легенде затраты всего + обе доли; «Разница» = поступления − МОРС − ТУЗИО.
  **Клик по элементу легенды** (27.08, `state.anHl`, span с `data-hl`:
  cost/mors/tuz/in) выделяет свой ряд на графике — он ярче/толще
  (`lineOp`/`lineW`/`bandOp`), остальные приглушаются; повторный клик снимает.
  Под линиями — таблица «По статьям» (26.08, третья итерация: столбцы с
  сегментами-статьями жили ~час и заменены как нечитаемые): накопительные
  затраты МОРС / **ТУЗИО (колонка с 27.08)** / поступления / разница
  (= КП − МОРС − ТУЗИО) по каждой статье за всё время, худшие отклонения
  сверху, пустая ячейка = статьи нет в источнике (контроль корректности);
  паркинг-/лобби-статьи КП схлопнуты в строки «Паркинг»/«Лобби» (`anKey`).
  Таблица на всю ширину экрана (27.08): ширины столбцов В ПРОЦЕНТАХ — числовые
  по 10%, кнопки 6%, остаток (~34%) — столбцу «Статья» (CSS `.anitems`).
  ⚠️ Пиксельные ширины (170/150px) не сработали: при `table-layout: fixed`
  браузер растянул все колонки пропорционально, «Статья» осталась узкой —
  не возвращать пиксели. В `AN_ITEMS` с 27.08 добавлены «Подсоба Шамов» и
  «Накладные расходы» — теперь сумма «Прогнозного бюджета» сходится с «Итого»
  вкладки «Бюджет» (18 статей, ~4 018,8 млн на 27.08).
  **Колонки «Прогнозный бюджет» и «Остаток»** (27.08): бюджет — свод экрана
  «Бюджет» (`state.budget`, `[статья, сумма, …]`, без фильтров; `loadBudget`
  дёргается из renderAnalytics при статусе idle и по готовности перерисовывает
  аналитику), статьи схлопнуты тем же `anKey`; статьи только из бюджета (без
  затрат/КП) тоже в таблице. Остаток = бюджет − МОРС − ТУЗИО **всегда по
  накопительным затратам за всё время** (при выбранном месяце колонки МОРС/ТУЗИО
  показывают месяц, а остаток — нет); пустой, пока свод не загружен или статьи
  нет в бюджете. Порядок колонок: Статья · Прогнозный бюджет · МОРС · ТУЗИО ·
  Остаток · Поступления (КП) · Разница · «Платежи».
  Статьи без поступлений сгруппированы внизу таблицы блоком **«Нет в бюджете
  с Заказчиком»** (26.08, строка `tr.an-noinc` с числом статей и суммами
  МОРС/ТУЗИО; при выбранном месяце блок пересчитывается за месяц; колонка
  ТУЗИО тоже подчиняется выбранному месяцу — tblMonths).
  Клик по строке (`state.anSel`) — линии сверху только по этой статье,
  повторный клик — все статьи. **Выбор месяца** (`state.anMonth`, 26.08):
  клик по точке или подписи месяца на графике — таблица «По статьям»
  показывает суммы этого месяца (вертикальная подсветка на графике, в
  заголовке «за ММ.ГГ», сброс — ссылкой «показать всё время» или повторным
  кликом). ⚠️ С 27.08 экран без пояснительных подписей (просьба пользователя):
  убраны подзаголовок под «Аналитикой», подсказка под графиком, комментарии
  под «По статьям» и заметка о выбранной статье — остались заголовки, легенда
  и ссылка сброса месяца; подсказки живут только в hover-title точек/легенды.
  «Платежи» открывают платежи только выбранного месяца (фильтр на
  фронте по «Дате проводки», месяц через new Date — не по началу ISO-строки,
  она смещена на часовой пояс). Кнопка «Платежи» у статей с затратами (26.08) —
  модалка со строками МОРС по статье (`action=morsRows`,
  `openAnPayments`/`anPaymentsHtml`; без кэша, лимит 2000 строк, свежие сверху;
  бэк фильтрует «Статью бюджета» той же нормализацией, что anKey; колонки
  Статья бюджета / Договор контрагента / Договор корректировка / Очередь
  скрыты — список `AN_PAY_HIDDEN` на фронте, в ответе бэка они есть).
  ⚠️ **МОРС — только очередь СБ3** (кол. H, просьба 26.08): в журнале смешаны
  СБ3 и СБ5, общие статьи («Отделка МОП», «Содержание …», «Отделка квартир
  MR Base») без фильтра завышались (~233 млн платежей СБ5). Фильтр в
  `buildAnalytics_` и morsRows (CONFIG.MORS_QUEUE/MORS_QUEUE_VALUE), кэш
  поднят до `analytics_v2`; затраты отделки после фильтра ~852,6 млн.
  Известное расхождение со сводником: статья МОРС «Отделка квартир MR Base»
  (~216 млн) включена — в своднике пользователя строка «(перенос)» её не
  подтянула и итог «Оплаты МОРС» там 869,6 млн против наших 1 085 млн.
  ⚠️ В строке 1 «Формы КП» две даты с опечаткой года — 2026-05-30 и 2026-06-29 стоят
  между 2025-04 и 2025-07 (видимо, 2025) — их деньги ложатся в 2026-05/06; плюс одна
  колонка между 2025-09-04 и 2025-09-29 вовсе без даты — не учитывается. Сообщено
  пользователю 26.08; после исправления заголовков — сброс кэша, график сам станет верным.

- **МОЛ** (`mol`, 2026-08-28, пункт под «Аналитикой»; 28.08 переименована из
  «Материалов» и полностью перестроена) — четыре материально ответственных
  (`MOL_LIST`: Овчинников, Смирнов, Джамал, Элшод — они же подрядчики собственных
  сил в поэтажке, имена совпадают с `BUDGET_SS_CONTRS`), у каждого два раздела:
  · **«Бюджет»** (`molBudget`) — суммы вкладки «Бюджет», но только по ячейкам этого
  МОЛ, по убыванию стоимости + «Итого». Колонки сгруппированы (просьба 28.08),
  шапка в ДВА ряда (класс `.mbud`, группы разделены `bsep`): **«Бюджет»**
  (Материалы = стоимость − работы · Работы cell[5] · Итого cell[2]) ·
  **«Факт (модель)»** (Материалы cell[8] · Работы cell[7] · Итого cell[6]) ·
  **«Личный кабинет»** (Итого). ✔ Проверено 28.08: факт материалы + работы =
  итог факта (Овчинников 81,0 + 136,7 = 217,7 млн). Всего по МОЛ: Овчинников
  412,2 млн (13 статей), Смирнов 214,4 (3), Джамал 352,0 (13), Элшод 61,0 (11).
  ⚠️ Шапка таблиц вкладки НЕ липкая (`position: static` для `.mtab th`) — при
  двух рядах sticky из `table.check` наезжала сама на себя; таблицы короткие.
  ⚠️ **Ширины колонок задаются на ячейках ПЕРВОГО ряда шапки** (28.08, вторая
  итерация): при `table-layout: fixed` раскладку определяет первый ряд с
  объединёнными ячейками групп, ширины второго ряда игнорируются — из-за этого
  суммы сначала обрезались («47 0…»). Сейчас: статья 19%, каждая группа из трёх
  колонок 30%, «Личный кабинет» 21% (`th.grphead[colspan="3"]`, `.grphead.lk`);
  у `grphead` НЕТ класса `num` — он давал ширину 11% всей группе.
  **Факт двумя колонками** (просьба 28.08): «модель» — «К оплате» из поэтажки
  (cell[6]), «ЛК» — лист «Личные_кабинеты» (`state.budgetLk`, МОЛ × статья).
  Расходятся сильно: Овчинников 217,7 против 48,5 млн, Смирнов 118,5/39,6,
  Джамал 134,7/62,6, Элшод 17,9/11,3. ⚠️ 9 статей ЛК не имеют пары в своде
  бюджета (паркинговые — в бюджете они схлопнуты в статью «Паркинг», плюс
  «СБ3 - Работы» и «Стены и перегородки» у Джамала): они выводятся отдельными
  строками с пометкой **«(только в ЛК)»**, иначе итог колонки терял бы ~13,5 млн.
  · **«Материалы»** — **Материал · Ед. изм · Объем итого по модели · Выполнено
  натурально · Остаток · К списанию ЛК · Стоимость материала итого**, по убыванию
  стоимости. Расчёт — по объёмам работ ИМЕННО ЭТОГО
  МОЛ (`molWorkVolumes`: ячейки бюджета `[подрядчик, корпус, стоимость, объём, …]`,
  ✔ проверено 28.08 — сумма объёмов ячеек работы совпала с «Объем работ» листа
  «Расходы» до сотых), умноженным на норму расхода из «Расходов»
  (`buildMaterialCalc`). Списание — из листа «Списание материалов» по этому МОЛ.
  ГКЛ на 28.08 (расчёт/списано, м²): Овчинников 95 968/85 549, Смирнов
  146 546/79 749, Джамал 76 999/53 422, Элшод 1 206/0.
  Списание грузится `action=writeoff` (`readWriteoff_`, кэш `writeoff_v1` 6 ч —
  лист маленький, обычный кэш), бюджет — уже имеющимся `action=budget`.
  **Все группы сворачиваемые** (просьба 28.08): клик по имени МОЛ
  (`data-mol`, `state.molOpen` — на старте ВСЕ свёрнуты, как на «Расценках»)
  и по заголовку раздела внутри (`data-sec` вида «Овчинников|budget»,
  `state.molSecClosed` — внутри раскрытого МОЛ разделы открыты, пока их не
  свернули). Свёрнутое содержимое не строится вовсе — страница лёгкая.

- **Материалы** (`materials`, 2026-08-28) — **как формируется стоимость материалов**:
  колонки **Материал · Ед. изм · Объем итого по модели · Выполнено натурально ·
  Остаток · К списанию ЛК · Стоимость материала итого** (сортировка по убыванию
  стоимости). «Остаток» (28.08) = объём по модели − выполнено натурально, есть и в
  расшифровке по работам, и на вкладке «МОЛ». ⚠️ Названия колонок уточнены 31.08
  по просьбе пользователя, чтобы источник читался сразу: «Объем итого по модели» —
  расчёт по «Расходам» (норма × объём работ), «К списанию ЛК» — колонка E
  «Материал к списанию итого» листа «Списание материалов» (в терминологии
  пользователя этот лист = личные кабинеты). Это НЕ лист `Личные_кабинеты` —
  тот в рублях по статьям и идёт в колонку «Факт (ЛК)» вкладки «МОЛ».
  ⚠️ В листе «Списание материалов» есть и колонка D «Объем материала итого»
  (по ГКЛ 384 170,73 против 218 719,14 в кол. E) — витрина её НЕ показывает.
  ⚠️ У вложенной таблицы-расшифровки ширины колонок ЗАДАНЫ ОТДЕЛЬНО
  (`table.inner th:first-child` 34%, `th.num` 9,4%): иначе она наследует
  `.mtab th.num` (13% × 7 = 91%) и название работы рвётся на шесть строк.
  «Списано итого» (28.08) — сумма по ВСЕМ МОЛ из листа «Списание материалов»,
  ключ тот же `matKey`, поэтому ГКЛ сравнивается целиком; вкладка сама дёргает
  `loadWriteoff`.
  **«Выполнено натурально»** (28.08) — та же норма расхода, но на ФАКТИЧЕСКИЙ объём
  работ: «Объем к закрытию» (кол. Z поэтажки) добавлен бэкендом в **cell[15]**
  ячейки бюджета (`buildBudget_`, кэш поднят до **budget_v29**, деплой **@61**);
  на фронте — `molWorkVolumes(mol, 15)` / `allFactVolumes()`, третий аргумент
  `buildMaterialCalc`. Есть и в расшифровке по работам. Пример ГКЛ на 28.08:
  расчёт 456 182,3 → выполнено натурально 255 436,9 → списано 218 719,1 м². Строка материала
  раскрывается (`state.matOpen`, `tr.mrow`/`tr.mdetail`) в расшифровку по работам:
  **Работа · Объем работ · Норма расхода на ед. · Объем материала · Цена за ед. ·
  Стоимость**. Источник — лист
  «Расходы»: норма «Расход в рачет» (кол. J, опечатка в заголовке), «Объем работ»
  (L), «Стоимость материала в ед работы» (K), «Расценка» (I). На 28.08 — 109
  материалов на 982,8 млн; топ: Квартирная дверь 154 млн, Дверь люк 92,7,
  ГКЛ 71,0, Керамогранит 1500х800 64,4.
  ⚠️ **Итог 982,8 млн НЕ равен колонке «Материалы» на «Бюджете»** (1 109,7 млн по
  статьям поэтажки, 1 618,6 млн со всеми доп-статьями): бюджет считает материалы по
  поэтажке (своя цена материалов на единицу + коэффициент переделки), здесь —
  по нормативной модели «Расходов». На одной работе («Монтаж перегородки ГКЛ 125 мм»)
  бюджет даёт 39,3 млн против 28,3 млн по модели — цена за единицу в поэтажке выше.

- Общее по обеим вкладкам: **все виды ГКЛ — одна строка**, и правило берётся
  **ИЗ ТАБЛИЦЫ** (решение 28.08, вторая итерация — регулярка по названию заменена):
  объединяется всё, у чего в справочнике «Материалы» колонка **«Тип материала» = ГКЛ**
  (`state.typeByMaterial` из `load`, `isGkl`/`matKey`, подпись `GKL_TITLE`).
  **Состав меняется прямо в листе «Материалы», код править не нужно.**
  На 28.08 тип ГКЛ проставлен у пяти позиций (ГКЛ 12.5, ГКЛВ 12.5, ГКЛ 9.5,
  ГВЛ 12.5, ГВЛ НГ 12.5), из них в «Расходах» встречаются три — итог
  **456 182,3 м² против 218 719,1 м² к списанию**. ⚠️ У «Гипрок Файер»
  (11 572,9 м²) тип не проставлен — идёт отдельной строкой; нужно включить —
  проставить ему тип ГКЛ в листе. Крепёж «Саморез для ГКЛ» и клей «Knauf Перлфикс»
  тип ГКЛ НЕ имеют (у Перлфикса его убрали 28.08) — в объединение не входят.
  Лист «Расходы» целиком лежит в `state.expenses` (приходит в `load`, отдельного
  запроса нет). Шапка страницы на обеих вкладках скрыта (как на «Аналитике»).
  ⚠️ Материалы без расчёта: нет в «Расходах» («Керамогранит 300х600 мм») либо
  пустой «Объем работ» («Штукатурка Инструмент и Расходники», «Штукатурка цементная
  Основит PK 27 M», «Керамогранит 400х400мм», «Клей для плитки Основит AC 15»).

- **ТУЗИО** (`tuzio`, 2026-08-31, пункт под «Аналитикой») — **оплата почасовщикам
  по месяцам и статьям бюджета**. Задача вкладки: понять, что входит в выплаты
  рабочим-почасовщикам в каждом месяце. Накопительная диаграмма
  (рукописный SVG, `renderTuzio`) — **в том же виде, что «Аналитика»**
  (переделана из столбиков 31.08.2026 по просьбе пользователя): заливки-полигоны
  по статьям, уложенные друг на друга накопительно с начала года, сверху линия
  общего итога (`#b42121`) с точками по месяцам. Точки и подписи месяцев
  кликабельны (`data-tzm`), у каждой точки прозрачный кружок r=13 — чтобы легче
  попасть мышью; у заливки в подсказке итог статьи за весь период. Статей 52 — цветом показываются
  **топ-12 по сумме за всё время** (`TZ_TOP`, палитра `TZ_COLORS`), остальные
  сведены в серую группу «Прочие статьи (N)» (`TZ_REST`); клик по элементу
  легенды (`data-tzg`, `state.tzHl`) оставляет ярким только выбранную статью.
  Клик по колонке, подписи месяца или сегменту (`data-tzm`, `state.tzMonth`) —
  расшифровка ниже. Переключатель `data-tzcum`/`state.tzCum`: **накопительно**
  с начала года (по умолчанию) или **только выбранный месяц**. Таблица
  расшифровки — статьи по убыванию суммы: Часы · Сумма · Табелей · Людей · Доля;
  клик по строке (`state.tzOpen`) раскрывает **состав статьи**: сотрудники (ФИО,
  часы и сумма накопительно, в скольких месяцах) и табели (№, дата, часы, сумма,
  людей; свежие сверху, первые `TZ_DOCS_LIMIT` = 300, о срезе написано в блоке).
  Шапка страницы на вкладке скрыта (как на «Аналитике»).

  **Два блока таблиц** (2026-08-31, вторая итерация): расшифровка месяца делится
  на «Статьи бюджета отделки» и «Справочно — нет в бюджете отделки» (`tzBlock`,
  признак `tzInBudget` = `anMatch`, то есть тот же список статей, которым
  фильтруется «Аналитика» — решение пользователя, чтобы цифры двух вкладок были
  сопоставимы). У каждого блока свой подзаголовок с числом статей, суммой,
  часами и долей от всех начислений; доля в строках считается **от общего итога
  периода**, а не внутри блока. Раскладка января–августа 2026: в бюджете отделки
  **20 статей, 54,3 млн**, справочно — **32 статьи, 172,3 млн** (крупнейшие:
  «Стены и перегородки» 79,1 млн, «Содержание площадки» 40,8, «СБ3 - Работы»
  19,9). ⚠️ Свод вкладки «Бюджет» (18 статей) — НЕ этот признак: дословно с ним
  совпадают только 15 статей ТУЗИО на 22,8 млн; разница — «Отделка МОП»,
  «Содержание отделки», «Содержание Джамал», «Содержание Овчинников»,
  «Отделка квартир MR Base».

  **Фильтр «Все работы / Отделка / Общестрой»** (`state.tzKind`, `data-tzkind`) —
  разметка **по смыслу работ**, независимая от бюджета (решение пользователя
  2026-08-31). Правило `tzIsFinish`: отделка, если в названии есть «отделк»,
  либо это МОП-статья (подстрока `- моп -`, покрывает СБ3/СБ5/Паркинг), либо
  «сделк» (сдельщики), либо «Содержание» + бригадир собственных сил
  (`TZ_FIN_CREWS`: Джамал, Овчинников, Элшод, Смирнов). Остальное — общестрой.
  Получилось **отделка 29 статей / 55,7 млн**, **общестрой 23 статьи / 170,9 млн**
  (контрольная сумма сходится с 226,6 млн). ⚠️ Спорные отнесения, если правило
  понадобится менять: «Стены и перегородки» (79,1 млн) — в общестрой, так как
  ГКЛ-перегородки отделки идут отдельной статьёй «СБ3 - Отделка MR Base ГКЛ
  перегородки»; «Содержание площадки» (40,8) и «СБ3 - Работы» (19,9) — тоже
  общестрой. Появится новая статья — проверить, куда её относит правило.

  **Сводная «статьи × месяцы»** (2026-08-31, `tzPivot`, между диаграммой и
  расшифровкой месяца): строки — статьи, столбцы — месяцы, в ячейках **часы
  за месяц, не накопительно**; справа столбец «Итого» по статье, внизу строка
  «Итого» по месяцам (сходится с помесячными часами: 63 638 / 60 204 / 88 825 /
  92 763 / 98 300 / 99 576 / 104 965 / 69 394 = 677 665 ч). Подчиняется тем же
  двум фильтрам, что диаграмма. Клик по месяцу в шапке выбирает его для
  расшифровки ниже, выбранный столбец подсвечен. Деление на бюджет/справочно
  здесь НЕ дублируется таблицами — статьи вне бюджета отделки помечены «спр.»
  прямо в строке. ⚠️ У таблицы своя раскладка (`table-layout: auto`): базовая
  `table.check` — fixed с `th.num` по 110px, 10 колонок в неё не помещались.
  Столбец статьи — 340px (втрое шире месячных, просьба 31.08), месячным задан
  потолок 110px.

  **Отдельный блок СБ5** (2026-08-31): статьи, чьё название начинается
  с «СБ5» (`tzIsSb5`, 12 статей, 7 309 ч), вынесены из основной сводной
  в **справочный блок в самом низу экрана, свёрнутый по умолчанию**
  (`state.tzSb5Open`, клик по заголовку раскрывает; `data-tzsb5`). Обе таблицы
  строит `tzPivotTable` — у неё есть флаг `bare`, чтобы для СБ5 вернуть только
  таблицу без своей шапки; `tzPivot` делит статьи и собирает блоки. Итоги:
  основная 670 356 ч + СБ5 7 309 ч = 677 665 ч.

  **Порядок блоков на экране** (2026-08-31, просьба пользователя):
  диаграмма → переключатель периода → «Статьи бюджета отделки» → «Справочно —
  нет в бюджете отделки» → сводная «статьи × месяцы» → справочный СБ5.
  Раньше сводная стояла сразу под диаграммой — перенесена вниз.

  **Раскрытие сводной** (2026-08-31, четвёртая итерация): строка статьи
  (`tr.tzpiv-item`, `state.tzPivOpen`) раскрывается в строку **«Без бригады»**
  (`tr.tzpiv-br`, `state.tzPivBrClosed` — хранит СВЁРНУТЫЕ, по умолчанию
  открыта), она — в строки сотрудников (`tr.tzpiv-emp`) с часами по месяцам.
  Клик по часам сотрудника в конкретном месяце (`td.tzpiv-cell`,
  `state.tzPivDay` = 'статья|человек|месяц') раскрывает под ним **подневную
  разбивку**: дата, № табеля, часы, сумма (`tzPivDaysRow`). Клик по ФИО
  открывает карточку сотрудника.
  ⚠️ Уровень бригады — **строка-заглушка «Без бригады», а не выдуманная
  группировка**. Решение пользователя 31.08.2026: «если нет бригады — пиши
  без бригады». Перед этим проверялись два способа восстановить бригады:
  кластеризация ЛЮДЕЙ по совместным табелям даёт мусор (бригада на 155
  человек), сцепка ТАБЕЛЕЙ по дням работает заметно лучше (48 бригад, 99%
  табелей ложатся в нитку, ядро 10–19 человек, но одна нитка слиплась
  из 165 человек) — от обоих отказались.
  Подневные данные берутся из **того же `action=tuzioPerson`**, что и карточка
  сотрудника (в ответе все его строки за все месяцы, фильтруем по статье
  и месяцу); ответы лежат в `state.tzPersonCache` (ФИО → ответ или строка
  `'error'`), поэтому карточка и разбивка греют кэш друг другу, а повторный
  клик после ошибки заново идёт на бэк. Пока ответ не пришёл — в строке
  «Загружаем дни сотрудника…».

  **Карточка сотрудника** (2026-08-31, третья итерация): клик по строке
  сотрудника в раскрытой статье открывает модалку (`openTuzioPerson`/
  `tzPersonHtml`) — итоги за весь период, разбивка по месяцам (табелей/часы/
  сумма), **свод по статьям бюджета** (статья · табелей · часы · сумма,
  добавлен 31.08.2026: по сотне строк табелей не видно, на чём человек
  работал) и списки табелей. **Табели раскрываются по клику** (31.08.2026):
  клик по статье в своде показывает под ней ЕЁ табели (`state.tzpOpenItem` —
  индекс строки в отсортированном по часам списке статей), заголовок «Все
  табели сотрудника (N)» раскрывает общий список (`state.tzpAllDocs`); по
  умолчанию свёрнуто и то, и другое. Перерисовка карточки — `tzRenderPerson`
  (модалка не собирается заново, берёт готовый ответ из `state.tzPersonCache`),
  клики ловит делегированный обработчик на `#modal-body`; чья карточка открыта,
  помнит `state.tzpFio`. Карточка всегда за весь период, независимо от
  выбранного на диаграмме месяца — об этом сказано прямо в ней.
  Данные — `action=tuzioPerson&p=<ФИО>`, ответ 13–14 КБ, холодный 10–20 сек
  (читается весь лист), дальше свой кэш на каждого сотрудника 6 часов.

  ⚠️ Блок **«Бригада — с кем работает»** был в карточке несколько часов и
  **убран с экрана 31.08.2026 по просьбе пользователя**. Бэкенд напарников
  по-прежнему считает и отдаёт в поле `mates` — понадобится вернуть, хватит
  вывода во фронте (описание механики ниже оставлено намеренно).

  ⚠️ **Как получается «бригада», если её нет в данных.** Колонка «Бригада»
  в экселе пуста во всех 8 месяцах, но структура табелей её выдаёт: человек
  за день попадает ровно в ОДИН ежедневный табель (в августе 2 исключения
  на 6129 пар «день+человек»), в табеле в среднем 11 человек (от 1 до 35),
  в день около 21 табеля. То есть табель за день = бригада за день. Поэтому
  бригада сотрудника — это люди, с которыми он чаще всего оказывается
  в одном табеле (`buildTuzioPerson_`: собираем его табели, вторым проходом
  считаем, кто ещё в них был; напарник в одном табеле дважды — две статьи
  в одном табеле бывают — считается один раз). Проверено на живых данных:
  у постоянного состава совпадение 65–95% табелей, дальше резкий спад, так
  что «постоянный состав» = напарники от половины табелей (в карточке они
  выделены и посчитаны). Наружу отдаём первых 60 напарников, общее число —
  в подписи.

  **Фильтр по статьям с мультивыбором** (`state.tzSel` — Set индексов, пусто =
  все; `state.tzSelOpen` — раскрыт ли список). Выпадающий список сделан
  **своими div'ами, а не `<details>`/`<label>`+checkbox**: у label клик
  дублируется (событие от label и от input), и счётчик переключался дважды,
  возвращаясь в исходное состояние. Список статей подчиняется фильтру вида
  работ (52 статьи / 29 при «Отделке»), клик мимо списка его закрывает,
  внутри — кнопки «Выбрать все» / «Снять все», кнопка «Сбросить фильтры»
  появляется, когда фильтры активны. При смене вида работ выбор статей
  **сбрасывается** — иначе таблица оказалась бы пустой (выбранные статьи
  в новый вид не входят). Оба фильтра действуют и на диаграмму: топ-12 статей
  пересчитывается по отфильтрованному набору.
  Данные — `action=tuzio` (`buildTuzio_`, кэш `tuzio_v1` 6 ч, чанкованный),
  ответ ~310 КБ, холодный расчёт ~9 сек. Грузится лениво при первом заходе
  (`loadTuzio`), при resize окна диаграмма перерисовывается.

  **Что считается оплатой почасовщику** (решение пользователя 2026-08-31).
  В экселе «Часы 2026» четыре вида документов, и один и тот же человек в один
  и тот же день попадает в ДВА табеля с разными часами:
  «Табель учета рабочего времени СКУД» (часы с проходной, 255,7 млн ₽ за 8 мес.)
  и «Табель ежедневный» (часы от мастера, 226,6 млн ₽). ⚠️ **Складывать их
  нельзя** — это двойной счёт (в августе у 498 человек из 498 дни пересекаются).
  Взят **ежедневный табель**: только в нём заполнена статья бюджета
  («Этап строительства», кол. C) — у СКУД она пуста в 100% строк. Дополнительно
  берутся только строки с заполненной **«Ставкой»** — это и есть признак
  «зарплата за час работы». Итог января–августа 2026: **58 871 строка,
  226 551 208 ₽, 677 665 часов, 52 статьи, 487 человек, 4 196 табелей**.
  Не входят: «Ведомость на перечисление официального заработка» (это выплата,
  а не начисление), «Премия» (сдельщики) и строки без ставки.
  ⚠️ **Бригад в исходном файле нет** — колонки «Бригада» и «Документ премия»
  пусты во всех 8 листах, поэтому вместо бригад показываются **номера табелей**
  (решение пользователя 2026-08-31).
  ⚠️ **Цифры вкладки НЕ сходятся с линией ТУЗИО на «Аналитике»** и не должны:
  там читается отдельный лист `ТУЗИО` с более широкими суммами (январь 66,8 млн
  против 21,2 млн здесь, июль 97,5 против 35,2) — в нём сидит что-то ещё кроме
  почасовщиков. Сводить одно с другим отдельной задачей не просили.

- **Проверки** — раскрывающийся блок в боковой панели (`#sb-checks-toggle`), внутри
  пункты-проверки с красными бейджами-счётчиками. Сейчас две:
  - **«Недостающие расценки»** (до 04.08.2026 — «Проверка расценки»;
    `screen='check-rates'`, `renderCheckRates`). Данные —
    **готовый лист «Проверки расценки»** (40×6: Подрядчик шахматка / Группа работ /
    Корпус / Работа / MIN и MAX из Этаж), отдаётся в `load` как `checkRates`; витрина
    его только показывает, сама ничего не вычисляет (решение пользователя 2026-08-03 —
    первая версия с вычисляемыми флагами жила ~час и заменена). Бейдж = число строк листа.
  - **«Отклонения расценок»** (до 04.08.2026 — «Расценки работ»;
    `screen='check-work-rates'`, `renderCheckWorkRates`, добавлена 2026-08-04). Считается **на странице**, без нового эндпоинта: работы,
    у которых базовая расценка отличается больше чем на 15% (`DEV_LIMIT`) от средней
    по подрядчикам ИЛИ от средней по собственным силам. Средние — по сводке
    `state.floors` (поэтажка: все ячейки подрядчик × корпус, СС отдельно от обычных);
    отклонение = (базовая − средняя) / средняя, в таблице с одним знаком после запятой,
    больше порога — красным. Сортировка от самого большого отклонения (по худшему из
    двух); работы сгруппированы по «Группа работ» (строки-разделители сворачиваются
    кликом, порядок групп — по худшему отклонению внутри, 04.08.2026). Клик по строке — под ней раскрывается
    список всех подрядчиков и СС с расценками (корпуса с одинаковой расценкой схлопнуты
    в одну строку). Зависит от
    `floors`: пока сводка грузится, экран показывает «загружается», бейдж пуст;
    `loadFloors` по готовности обновляет бейдж и, если вкладка открыта, таблицу.
  На экранах проверок шапка с фильтрами скрывается. Новые проверки добавлять: пункт
  в `#sb-checks` + запись в справочник `CHECK_SCREENS` (имя → render-функция) +
  render-функция по образцу.

## Архитектура коротко

- **Масштаб текста таблиц** (2026-08-14): кнопки **А− / 100% / А+** в шапке справа
  (перед поиском) меняют размер шрифта всех ячеек th/td от 60% до 120% шагом 10%
  (единый размер `12px × масштаб` через `!important`-правило в динамическом `<style>`,
  `applyZoom`); ширины колонок не меняются — уменьшение шрифта позволяет увидеть
  длинные значения целиком. Хранится в localStorage `otdelka_zoom`, действует на всех
  экранах (на «Бюджете»/«Проверках» шапка скрыта, но масштаб применяется).

- **Auth**: общий пароль (не индивидуальные токены) → localStorage (`otdelka_token`) → бэк сверяет `t=` с Script Properties `PASSWORD` на каждый запрос.
- **doGet?action=load** — единственный эндпоинт с данными:
  `{ works, expenses, materials, contrRates, ssRates, ssMap, checkRates }`, ~800 КБ
  (contrRates = «Расценки подрядчиков» 810 строк, ssRates = «Расценки СС» 98,
  ssMap = «Справочник СС» 141 — добавлены 2026-08-03 для деталей по клику на подрядчика). Из `works` бэкенд отсекает строки с пустым
  `Название работы из АР`: в листе ~818 строк-призраков, где заполнена только ячейка
  «Плановый процент переделки» (формула растянута по столбцу). После отсева работ **321**,
  ответ похудел с 960 до 548 КБ. Понадобятся эти строки — снять фильтр в `load`.
  Там же отсекается **место «1 этаж»** (`CONFIG.EXCLUDE_PLACES`, решение пользователя
  2026-08-03) — это 190 работ из 321. Итого витрина видит **131 работу**, 20 групп,
  4 места (Квартира, Лестница, МОП, Технические помещения), 58 работ с признаком
  ген. объёма; ответ 487 КБ.
- **Скрытые колонки работ** (список `HIDDEN_COLS` в `index.html`, решение пользователя
  2026-08-03): Название работы СС, ИД факт, три «Признак для бюджета…», Признак Уровень 1
  ДГП для увязки, Материалы. Данные приходят с сервера, но не показываются и не участвуют
  в поиске.
- **Колонка «Ген. объем»** (2026-08-03) — галочка ✓ там, где заполнен
  `Признак для бюджета дополнительный объем` (`hasVolume()`), сразу за названием работы.
  ⚠️ В этой колонке Sheets лежит **не «да/нет», а название группы** (Стяжка, Окраска…):
  заполнено у 73 работ из 321, у 53 из них значение совпадает с «Группой работ».
  Признак означает «работу учитывать при расчёте объёма».
- **Раскладка экрана — 1/3 + 2/3** (2026-08-03, решение пользователя; раскрытие строк
  внутри списка убрано): `main` — flex-колонка, `#pane-top` (33%, своя прокрутка) — дерево
  работ, `#pane-bottom` (остальное, своя прокрутка) — закреплённая панель «Подрядчики и
  собственные силы». Клик по работе = **выбор** (подсветка `.sel`, панель наполняется через
  `selectWork`/`renderBottomPane`); если фильтр/поиск убрал выбранную работу из списка —
  выбор снимается, панель возвращается к подсказке. **Материалы — в нижней панели справа
  от подрядчиков** (`#bottom-cols` — flex из двух `.bottom-col`, обе наполняются в
  `renderBottomPane`; каждая колонка со своей горизонтальной прокруткой). Всплывающее
  окно материалов и ссылка в «Баз. расценка мат.» существовали ~час 2026-08-03 и убраны
  по просьбе пользователя. Заголовков в нижней панели нет вовсе (ни «Материалы», ни
  названия работы — работа видна по подсветке в дереве); подпись «расценка за работу
  на ед. в бюджет, руб» — под таблицей подрядчиков, не над ней.
  В таблице материалов короткие заголовки (2026-08-03): «Ед. изм», «Итого»
  (= лист-колонка «Расход в рачет») и «Стоимость» (= «Стоимость материала в ед работы»;
  ключи данных прежние, `SUM_COL` не менялся). Колонка названия материала — 320px (было 460).
- **Сводная по подрядчикам** — в нижней панели (`contractorsHtml()`).
  Строки — подрядчики, столбцы — **всегда все 12 корпусов** (`ALL_CORPS`, 2026-08-03 —
  раньше показывались только корпуса конкретной работы и колонки «скакали» между работами).
  Строка **«Базовая расценка»** (значение «Базовая расценка работа в расчет, руб» из строки
  работы) — **всегда нижней строкой** таблицы, но заполнена только в корпусах, где не
  назначен ни подрядчик, ни собственные силы (`occupied`; уточнение пользователя
  2026-08-03 — раньше показывалась только когда подрядчиков не было вовсе). Строка
  приглушённая (`tr.baserow`), не кликабельна. Если у работы нет ни подрядчиков,
  ни базовой расценки — пояснительная заметка вместо таблицы.
  Работы с ген. объёмом внутри группы отсортированы **по «Объем работ» на уменьшение**.
  **Строки подрядчиков кликабельны** (2026-08-03, `tr.crow` → `contractorDetailHtml`,
  показ — **в модальном окне** `#modal-overlay`; вариант с блоком под таблицей жил ~час
  и заменён по просьбе пользователя; закрытие — крестик/Esc/клик мимо): обычный подрядчик —
  строки листа «Расценки подрядчиков» по работе+подрядчику (корпус, этажи с/по,
  включить/исключить, работы и материалы за ед.); из блока «Собственные силы» —
  лист «Расценки СС» (Подрядчик · Работа · на руки · с налогами · накладные · итого).
  Мэппинг СС (финальная логика пользователя, 3-я итерация 2026-08-03): названия работы СС
  берутся из **ПОЭТАЖКИ, кол. J «Название работы СС»** (в листе «Работы» колонка почти
  пустая — из-за этого первые версии «ничего не находили»; лист «Работы» остался запасным
  источником). `action=floors` теперь отдаёт `{floors, ssNames}` — `ssNames` = работа →
  уникальные названия СС из поэтажки (кэш-ключ поднят до `floors_v2`). Дальше набор
  расширяется через «Справочник СС» в обе стороны (A «Работа СС» ↔ C «Работа для расценок
  модель отделка АР») и ищется в «Расценки СС».«Работа для АР».
  ⚠️ Для СС подрядчик-фильтр НЕ применяется — показывается весь перечень работ СС по
  выбранной работе (указание пользователя). Имена подрядчиков Расценок СС =
  Джамал/Овчинников/Элшод/Смирнов (как в поэтажке).
  Покрытие (проверено): детали подрядчиков — у 86 работ из 131, СС — у 38 из 131;
  контрольный случай «Монтаж перегородки ГКЛ…125 мм» → «ГКЛ перегородки» → 4 строки
  (все четыре СС-подрядчика). Иначе — пояснение, чего не хватает.
  Выбор подрядчика сбрасывается при смене работы. Заголовки обеих нижних таблиц на одной высоте
  (`#bottom-cols table.inner { margin-top: 0 }`).
  в ячейках — **расценка за работу на единицу в бюджет**, а не сумма: расценки складывать
  нельзя (выбор пользователя).
  **Сверка с ведомостью** (2026-08-14): расценка ячейки (из поэтажки) сверяется со
  строками «Расценок подрядчиков» по работе+подрядчику+корпусу; не совпала ни с одной
  (допуск 0,005; учитывается режим Работа/Материалы/Работа+материалы) — ячейка красная
  (`td.rate-bad`), в подсказке — значения из ведомости. Нет строк ведомости по
  комбинации — не подсвечиваем (сверить не с чем). СС-блок не сверяется. Проверено: внутри тройки работа+подрядчик+корпус расценка
  всегда одна — 0 расхождений на 1073 тройки, поэтому значение берётся как есть.
  **Собственные силы** — по решению пользователя это строки с заполненной
  «Расценка СС работа» (отдельного подрядчика «СС» в данных нет). Признак считается
  по строке, а не по подрядчику: у Смирнова 596 строк СС и 88 обычных.
  С 2026-08-03 СС-строки — **отдельный блок «Собственные силы»** внизу той же таблицы
  (строка-разделитель `tr.sect`), подписей у имён нет; подрядчик с обоими типами расценок
  (Смирнов) появляется в обоих блоках с разными ячейками.
  Строки без указанного подрядчика в сводную не попадают (их 14107, 248 млн) — сводная
  именно про подрядчиков.
  Покрытие: сводка есть у **81 работы из 131**; у остальных выводится пояснение,
  что подрядчиков в поэтажке не нашлось.
- **Итог по группе объёма — удалён** (пробовали разные варианты 03.08.2026: сумма без
  условий → только по работам с ген. объёмом → раздельно по единицам измерения из-за
  смешения м2/м.п. → в итоге пользователь попросил вообще убрать сумматор для группировки
  по поверхности; когда группировка стала Место→Группа работ, сумматор решили не возвращать).
  `hasVolume()` и колонка **«Ген. объем»** (галочка в строке работы) остались — они
  независимы от группировки и просто показывают признак у конкретной работы.
- **Тема — светлая** (решение пользователя 2026-08-03, было тёмное оформление, переключателя
  нет). Цвета — переменные в `:root` (`--bg`, `--panel`, `--panel2`, `--border`, `--text`,
  `--text-dim`, `--accent`, `--danger`); менять тему целиком — там. Несколько мест были
  зашиты напрямую в hex (заголовок группы, наведение на группу, фон раскрытого списка) —
  тоже переведены на светлые тона.
- **Группировка — три уровня: Место -> Поверхность -> Группа работ** (2026-08-03,
  финальная версия после нескольких итераций: группировка по «Группе работ» →
  по «Поверхности» → вложенная Место→Группа работ → текущая, с добавленным средним
  уровнем «Поверхность»). `groupWorks()` строит места (4: Квартира, Лестница, МОП,
  Технические помещения), внутри каждого — поверхности (3–5 на место), внутри
  каждой поверхности — подгруппы по `Группа работ`. Порядок везде — как в листе,
  не по алфавиту. Ключи для сворачивания составные и уникальны на всех уровнях:
  место — `p:Название`, поверхность — `p:Место›s:Поверхность`,
  подгруппа — `p:Место›s:Поверхность›g:Группа`
  (`allGroupKeys()` — плоский список всех ключей трёх уровней, нужен для
  «свернуть/развернуть всё» и для стартового состояния).
  Колонки `Место`, `Поверхность`, `Группа работ` скрыты в `HIDDEN_COLS` — их значения
  в заголовках уровней, не в строках. Тег в конце строки остался один — **Локация**
  (`TAG_COLS`/`TAG_LABEL`).
  **На старте всё свёрнуто** (все три уровня), кнопка «Развернуть все / Свернуть все»
  рядом с фильтрами разворачивает/сворачивает все три уровня разом.
  CSS-классы заголовков: `tr.plc` (место, самый жирный), `tr.srf` (поверхность,
  отступ 24px), `tr.grp` (группа работ, отступ 40px, самый лёгкий шрифт).
  Сверху три выпадающих фильтра (Группа работ · Место · Поверхность — **независимы
  от группировки списка**, это отдельный механизм отбора строк, значения из данных,
  сортировка `localeCompare('ru')`) + «Сбросить»; комбинируются с поиском.
  В счётчике — «N из M · мест: K · поверхностей: L · групп: M».
  Колонка `Название работы из АР` переносится по словам (класс `wname`), остальные — в одну строку.
  **Сумматор по объёму — только на уровне «Группа работ»** (просьба пользователя 2026-08-03;
  у мест и поверхностей его нет, там только счётчик работ): сумма `Объем работ` по работам
  с галочкой ген. объёма, раздельно по единицам измерения (`g.volParts` — массив пар
  [единица, сумма]). Сейчас внутри групп единицы однородны (проверено: 0 смешанных из 25
  групп с галочками), раздельный подсчёт оставлен на случай изменения данных.
  Сумматор появляется у 25 групп из 44 — у остальных нет работ с галочкой.
  Выводится **в колонках таблицы, а не рядом с названием группы**: число — в колонку
  `Объем работ`, единица — в колонку `Ед. изм. работа` (класс `gsum`). Если единиц вдруг
  окажется несколько — всё склеивается через « + » в колонке объёма.
- **Отрисовка дерева**: строки заголовков уровней — не одна ячейка с `colspan`, а полный
  набор ячеек по колонкам (иначе итог некуда положить). Название уровня лежит в колонке
  `Название работы из АР` (класс `gname`), название работы — там же (класс `wname`).
  Лесенка задаётся **только через `padding-left` этой колонки**: место 8px,
  поверхность 24px, группа работ 40px, работа 56px, раскрытый список материалов 72px.
  ⚠️ Стрелки ▸/▾ у всех уровней **и у работы** лежат внутри ячейки с названием; первая
  служебная колонка `td.chev-cell` — пустая и узкая (8px), она нужна только чтобы число
  ячеек совпадало с пустым первым `th`. Раньше стрелка работы жила в широкой (48px)
  служебной ячейке — из-за этого работа отрисовывалась левее заголовка своей группы
  и иерархия «ломалась»; при правке отступов это надо держать в голове.
- **Оформление таблицы работ** (2026-08-03): `Место`, `Поверхность`, `Локация` не отдельные
  колонки, а одна ячейка с тегами-пилюлями (`TAG_COLS` / `cellHtml`) **последней колонкой**;
  фильтры и поиск при этом работают по исходным колонкам. Слева у работы только треугольник —
  счётчик материалов оттуда убран (в узкой ячейке он обрезался в «…»).
  Денежные колонки (`MONEY_HINT`: расценка, цена, стоимость, руб) — всегда 2 знака после
  запятой, остальные числа — до 4.
  ⚠️ **Обе таблицы — `table-layout: fixed`** (жалоба пользователя 2026-08-03: «столбцы
  скачут в зависимости от открытых уровней»). Это значит: ширины берутся **только из `th`**
  и не зависят от содержимого видимых строк. `width`/`min-width`/`max-width` на `td`
  игнорируются — задавать ширину надо в блоке `thead th …` (и `table.inner th …`),
  иначе правка «не сработает». Текущие ширины: служебная колонка 8px, название работы 38%,
  «Ген. объем» 62px, локация 200px, все остальные 96px; в списке материалов — название
  материала 460px, остальные 84px, ширина самой таблицы 964px.
  В списке материалов «Ед. изм. материала» и «Слои» той же ширины, что числовые
  (флаг `fix: true` в `DETAIL_COLS` → класс `fixw`).
  **Выравнивание** (просьба пользователя 2026-08-03): всё по центру — и заголовки, и данные,
  в обеих таблицах. По левому краю только название работы (`wname`), локация (`tags`),
  название материала (`mname`) и подпись «Итого материалов на ед. работы».
  Числовые колонки тоже центрированы (`.num` — `text-align: center`, было `right`).
  Короткие заголовки — словарь `RENAME` в `index.html` (ключ = имя в Sheets):
  «Ед. изм. работа» → **Ед. изм**, «Базовая расценка работа в расчет, руб» →
  **Баз. расценка раб.**, «Стоимость материалов базовая, за ед» → **Баз. расценка мат.**
  Переименование только на витрине; числовое/денежное форматирование — по исходному имени.
  **Порядок колонок жёсткий** — массив `COL_ORDER` (решение пользователя 2026-08-03):
  Название · Ген. объем · Ед. изм · Баз. расценка раб. · Баз. расценка мат. · Объем работ ·
  Локация. «% переделки» с витрины **убран** (ключ в `HIDDEN_COLS`); новые колонки Sheets
  теперь сами НЕ появляются — их нужно добавлять в `COL_ORDER` руками.
  «Объем работ» форматируется **до десятых** (`fmtVol`, и в строках, и в итогах групп).
  Внутри группы работы **с признаком ген. объёма — сверху, без — снизу**
  (стабильный sort в `groupWorks`; внутри половин порядок листа сохраняется).
- **Колонки раскрытого списка материалов** — жёстко заданы в `DETAIL_COLS` (`index.html`),
  порядок по просьбе пользователя 2026-08-03:
  Материалы · **Ед. изм. материала** · Слои · **Расход** · **Расход итого** · Расценка ·
  Стоимость материала в ед работы, внизу строка **«Итого материалов на ед. работы»** —
  сумма по колонке `Стоимость материала в ед работы` (`SUM_COL`).
  «Ед. изм. материала» берётся из справочника `Материалы` (колонка `Ед. изм`) по названию
  материала: совпало 531 из 547 строк, у 16 в справочнике пусто — ячейка пустая.
  Единица измерения работы в списке не дублируется — она есть в строке работы выше.
  Не показываем: Место, Тип работ, Объем работ, Объем материала, Название работы из АР.
  «Расход» — вычисляемая колонка: берём «Ручной расход», если он заполнен, иначе
  «Расход автомат» (в данных ручной заполнен всего в 8 строках из 1010, автомат — в 547;
  у 463 строк пусты оба, там колонка пустая). «Расход итого» — это лист-колонка
  «Расход в рачет» (опечатка в названии — на стороне Sheets, переименована только на витрине).
- **Раскрытие материалов**: индекс `Map(название работы → строки «Расходов»)` строится один
  раз после загрузки; клик по строке вставляет вложенную таблицу (все колонки «Расходов»,
  кроме названия работы). У 266 работ из 321 материалы есть (в среднем 2,1, максимум 11),
  у 55 — нет, для них подпись «Материалы … не найдены». Клики ловятся делегированием
  на `tbody`, `onclick` в разметку не вшивается.
- **doGet?action=ping** — диагностика (тоже требует пароль).
- **doGet?action=clearCache** (2026-08-14) — сброс всего серверного кэша (та же
  `clearCache()`, что в редакторе GAS). На витрине — **кнопка «⟳ Обновить данные»**
  внизу боковой панели (`#sb-refresh`): сбрасывает кэш и перезагружает страницу,
  данные перечитываются из таблицы. Первая загрузка после сброса медленнее
  (floors/vols/budget пересчитываются). Появилась из-за случая с Овчинниковым:
  подрядчика внесли в поэтажку, а витрина 6 часов показывала старую сводку из кэша.
- **doGet?action=floors** — сводка «подрядчик × корпус» по каждой работе (2026-08-03).
  Считается по листу `Поэтажка_работы` функцией `buildFloorSummary_()`: читаются **только
  6 колонок** (Работа, Корпус, Подрядчик сводный, Расценка за работу на ед в бюджет,
  Расценка СС работа, Стоимость материалов на ед в бюджет — последняя добавлена
  04.08.2026 для переключателя «Работа/Материалы») и сразу сворачиваются в агрегат
  `{ работа(lowercase) : [[подрядчик, корпус, расценка работы, признакСС, расценка
  материалов], …] }` — ответ ~25 КБ.
  Холодный расчёт ~25 сек, поэтому результат кладётся в `CacheService` на 6 часов
  (ключ `floors_v3`, из кэша 1.5 сек). Сброс — функция `clearCache()` в редакторе GAS;
  **новый ключ кэша обязательно добавлять туда же**. В `load` сводку не включаем —
  проверено: она удлиняла загрузку витрины с 6.5 до 30 сек. Фронт грузит её фоном
  (`loadFloors()`), уже раскрытые работы после прихода данных перерисовываются
  (`refreshOpenDetails()`).
- **doGet?action=budget** — свод для вкладки «Бюджет» (2026-08-04): по `Поэтажка_работы`
  сумма «Стоимость материалы + работа для бюджета» (кол. AG) в разрезе «Признак для
  бюджета основной» (кол. L). Функция `buildBudget_()`, ответ — `[[статья, сумма], …]`
  по убыванию (суммы округлены до рубля), строки с деньгами без статьи — в «— без статьи —».
  Кэш `budget_v18` на 6 часов, сброс той же `clearCache()` (и при фиксации новой
  базы в saveBaseline). В ответе также `lk` —
  лист «Личные_кабинеты» целиком (35 строк, `readLk_`), `kp` — свод «Формы КП»
  статья → закрытие у заказчика (`readKp_`) и `base` — суммы базового расчёта
  по работам для колонки «Базовый бюджет» (`readBaseSums_`, 2026-08-25);
  в budget подмешаны статьи «Накладные расходы» (`readOverhead_`, 2026-08-25)
  и «Лобби» (`readLobby_`, 2026-08-25, лист «Лобби»). С 2026-08-18 в ячейке
  стоимость работ AE (cell[5]) для раздельного показа работ/материалов в сводной;
  с 2026-08-21 — факт «К оплате» AA (cell[6]) и его разбивка: факт работы Z×AB
  (cell[7]) и факт материалы Z×AC (cell[8]). Ячейка: `[подрядчик, корпус,
  стоимость AG, объём, коэф, стоимость работ AE, факт AA, факт работы, факт мат.]`.
  ⚠️ Строки, где стоимость AG пустая/0, в свод не попадают вовсе — факт по таким
  строкам (если вдруг есть) в колонки не войдёт.
- **doGet?action=tuzio** (2026-08-31) — свод вкладки «ТУЗИО» по листу «ТУЗИО_часы»
  (`buildTuzio_`): месяцы, словари статей и ФИО, три разреза (статья×месяц,
  статья×месяц×человек, статья×месяц×табель). Сырые строки наружу не идут.
  Ответ ~310 КБ, холодный ~9 сек, кэш `tuzio_v1` кусками на 6 часов.
- **doGet?action=tuzioPerson&p=<ФИО>** (2026-08-31) — карточка одного сотрудника
  (`buildTuzioPerson_`): его строки [месяц, день, табель, статья, часы, сумма],
  напарники по числу общих табелей (=бригада) и итоги. Ответ 13–14 КБ,
  холодный 10–20 сек (читается весь лист), кэш свой на каждое ФИО
  (`tuzio_p_v1_` + base64 имени) на 6 часов.
- **doGet?action=questions** — список вопросов по работам из JSON-файла на Диске
  (2026-08-04). Без кэша — файл маленький.
- **doPost** — единственный POST (2026-08-04), тело — JSON в `text/plain` (обход
  CORS-preflight): `{ t, action: 'addQuestion'|'setQuestionStatus'|'saveBaseline'|'saveFact'|'importShamov'|'importTuzio', ... }`
  (importShamov — заливка листа «расчет_Шамов» целиком, 2026-08-24;
  **importTuzio** — заливка листа «ТУЗИО_часы», 2026-08-31: `mode:'start'` очищает лист,
  растит его до нужной высоты, ставит текстовый формат колонкам «Месяц»/«Табель» и пишет
  заголовок, затем `mode:'append'` дописывает порции по 5 тыс. строк — 59 тыс. строк
  за один POST не проходят. Запускается скриптом `Приложения/tuzio_import.py`).
  Вопросы пишутся в JSON-файл под `LockService` (защита от одновременных записей). ID файла — в Script
  Properties `QUESTIONS_FILE_ID`, создаётся `questionsFile_()` при первом обращении.
  **`saveFact`** (2026-08-10) — пакет отметок `{ author, marks: [{work, corp, floor, pct}, …] }`
  (лимит 300) дописывается журналом в лист «Факт» (создаётся `ensureFactSheet_` при первой
  записи); pct зажимается в 0..100, строки экранируются `safeCell_()`; ветка стоит ДО
  чтения файла вопросов — Диск для неё не нужен. Это **единственная запись в Sheets**.
- **doGet?action=fact** — актуальные отметки факта из журнала «Факт»:
  `{ 'работа|корпус|этаж': [процент, кто, когдаISO] }` (последняя строка по ключу).
  Без кэша — лист небольшой, после записи данные должны быть свежими (2026-08-10).
- **doGet?action=factRef** — справочный факт из поэтажки для подсказок вкладки «Факт»:
  работа → корпус → [[этаж, готовность % (средневзв. по объёму, доли автоматически
  умножаются на 100), объём к закрытию], …]. Тяжёлый расчёт, кэш чанкованный
  `factref_v1` 6 ч (2026-08-10).
- **doGet?action=probe** — разведка `Поэтажка_работы`: только агрегаты (уникальные значения
  подрядчиков и корпусов, заполненность денежных колонок, проверка однозначности расценок).
  Данные наружу не отдаёт, ответ ~1.5 КБ. Оставлен для будущих сессий — им удобно изучать
  тяжёлый лист, не нарушая правило «данные в чат не тянуть».
- **doGet?action=meta** — оглавление таблицы: имена всех вкладок, число строк/колонок,
  заголовки колонок. **Без единой строки данных** — ответ ~3 КБ, безопасно смотреть из чата
  (см. раздел про лимит выше). Фронтом не используется, нужен для диагностики и сессий Claude.
- Запись из витрины: вопросы — в JSON-файл на Диске (2026-08-04), отметки факта —
  журналом в лист «Факт» (2026-08-10, `saveFact`). Существующие строки и листы
  скрипт не изменяет и не удаляет никогда.
- Рендер таблиц — чанками по 300 строк через `requestAnimationFrame`, чтобы не подвешивать вкладку на ~1000+ строк.
- Поиск — по всем колонкам сразу, debounce 150мс, без запроса к серверу (все данные уже на клиенте).

## ⚠️ Данные в чат не тянуть (инцидент 2026-08-02)

Ответ `?action=load` — **~980 КБ** (Работы 1139×17, Материалы 273×6, Расходы 1010×13),
это ~300–500 тыс. токенов. Один вывод этого ответа в переписку съедает весь дневной лимит
Claude за один запрос. Боевая «Отделка СБ3 база» (12 МБ) — ещё хуже.

Правила для будущих сессий:
- **Никогда** не печатать ответ `load` (и любой дамп листов) в чат целиком — ни через
  `Invoke-WebRequest`, ни через `WebFetch`, ни через MCP Google Drive.
- Нужно проверить, что бэк жив → запрашивать `?action=ping` (ответ ~60 байт).
- Нужно узнать структуру/объём → выводить только агрегаты: `.Length`, `.Count`,
  список названий колонок. Пример безопасной команды:
  ```powershell
  $r = Invoke-WebRequest -Uri "<URL>?action=load&t=<пароль>" -UseBasicParsing
  "bytes: " + $r.Content.Length
  ($r.Content | ConvertFrom-Json).works.Count
  ```
- Нужно посмотреть примеры строк → максимум 3–5 строк, не весь лист.
- Если данные нужно анализировать всерьёз — сохранять в файл в scratchpad и работать
  запросами по файлу (фильтр/подсчёт), а не читать файл целиком.

## Известные нюансы

- **Инцидент 2026-08-25: IMPORTRANGE в «Отделка ИИ» падал в `#REF!`** (листы
  «Поэтажка_работы», «Личные_кабинеты», «Проверки расценки») при перестройке
  боевой «Отделка СБ3 база» — витрина при этом молча теряет все статьи
  «СБ3 - …», накладные, переделки и колонку ЛК (бэкенд видит лист 4×1).
  Симптом: на «Бюджете» остались только статьи из отдельных листов (Паркинг/
  Лобби/Шамов). Диагноз — `?action=meta`; лечится правкой формул IMPORTRANGE
  в «Отделка ИИ» (чинит пользователь), затем сброс кэша.
- В таблице появился лист «Факт экспорт» (~999×6, 25.08, создан пользователем) —
  витриной не используется; лист «Факт» (журнал отметок) при этом стал ~3 строки.

- **Новый GAS-проект после `clasp deploy` не сразу доступен анонимно** — даже с `"access": "ANYONE_ANONYMOUS"` в манифесте, реальный доступ пришлось один раз вручную подтвердить в редакторе (Развертывание → Управление развертываниями → карандаш → проверить «У кого есть доступ» → Развернуть). Для существующих проектов (передеплой через `update-deployment`) этот шаг не нужен.
- **`clasp run` не работает** — «Google Apps Script API» не включён для аккаунта `kuzkin@acons.group` (Script function not found / API executable). Функции типа `setup()` в Script Properties приходится запускать вручную через редактор (▷ «Выполнить» → выбрать функцию → авторизовать).
- Если понадобится сменить пароль — поменять в `script.gs` функции `setup()` и один раз выполнить её заново в редакторе, либо вручную через «Настройки проекта → Свойства скрипта» в редакторе GAS.
- GitHub Pages и включение доступа GAS webapp настраивались вручную в вебе (не через API — не было ни `gh` CLI, ни валидного GitHub-токена на момент разработки).

## Что не сделано (осознанно, на будущее)

- Редактирование расценок из панели (изначальный запрос пользователя) — отложено, пока только просмотр.
- ИИ-проверка расценок по рыночным ценам из интернета — отложено до второй итерации.
- Роли просмотр/редактирование — пока один общий пароль без ролей.
